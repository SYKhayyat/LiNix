I'm working on LiNix, a Rust "manager of managers" CLI. Repo: C:\Users\Administrator\Videos\Nexus\linix (branch v6). Release binary already built at ./target/release/linix.exe. You are running elevated (admin) — that's intentional.

Task: finish validating/fixing the choco (Chocolatey) backend on Windows. Every other backend (scoop, npm, pnpm, yarn, bun, pipx, uv, gem, winget, cargo) is already proven green non-elevated. choco is the only admin-gated one.

Already fixed + committed (7c97d5c): choco's package id is wget but choco list -lo -r returns the Title Wget|1.21.4. GenericQueryable::info (in src/backends/generic.rs) matched case-sensitively, so remove no-op'd. I made info match case-insensitively for choco/scoop/winget. Proven non-elevated: ./target/release/linix.exe -y remove choco:wget logs LiNix: Purging wget from choco... (info matched).

The anomaly to root-cause: running elevated, the same binary logs LiNix: 'choco:wget' is not installed under any managed backend — i.e. info() returns None — even though, in the same elevated run, linix -b choco list returns Wget|1.21.4. Non-elevated it matches; elevated it doesn't. info() calls list_installed() (runs choco list -lo -r, parsed by parse_choco_list in src/parsers/windows.rs as name|version). Reproduce it, find why info's list_installed yields no case-insensitive wget match under admin (add temporary eprintln! debug if needed), fix if it's a linix bug, then rebuild and confirm the full lifecycle: install choco:wget → list shows it → remove → gone from list + manifest.

Key flow — handle_remove in src/main.rs (~L348-378): for choco:wget, bare_name="wget"; loops backends; if q.info(&bare_name).await?.is_some() → runs inst.remove + remove_package_from_local; else warns "not installed under any managed backend".

Constraints/gotchas:
- scoop refuses to run as admin — do NOT run the full harness (scripts/integration-windows.sh); test choco directly/in isolation.
- Use wget as the test package, NOT jq — choco flags jq's package as broken (checksum mismatch that only recovers interactively).
- If shelling to bash, choco/coreutils PATH: prepend /usr/bin (scoop's busybox shims otherwise shadow head/tee/grep).
- linix leaks a big SystemRestore JSON blob to stdout during snapshot maintenance when elevated — cosmetic, ignore/redirect.
- Keep cargo test --lib green (141 tests). End commit messages with Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>.

Start by reproducing the elevated info-returns-None while list shows Wget.
