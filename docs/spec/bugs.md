# Part VI — Bugs

*[Shall v7](../SPEC.md) — the map is there; this is one part of it.*

## VI.0 The worst one, at the top because a table row was not enough

**FIXED 2026-07-23 — the line is deleted, not guarded, and the four tests that prove it are
below.** `heal()` no longer removes on any branch of the install path; a `--dry-run` reports the
recovery it would perform and performs none of it, and takes the data lock like every other
writer. What follows is kept as written because the reasoning is the reason for V.64 and for the
rule in II.10, and because the *habit* it indicts is not fixed by a commit.

**S24 — Shall uninstalls software nobody asked it to uninstall, and no refusal in this document
applies.** Not a design tension, not a deferred trade-off: a package that is installed,
declared, wanted and protected can be removed by a command that was asked to install something
else. It happened on 2026-07-23, on the owner's own machine, to Google Chrome, from
`shall -y install nimble:nimjson`.

**Why it is the worst bug here and not merely a bad one.** Every other removal defect in this
document is a removal that was *wrong*; this is a removal that is **unaccountable**. It does not
appear in `plan`. It does not appear in `--dry-run` — worse, `--dry-run` performs it (**S25**).
It is not counted, so `max_removals` never sees it. It is not checked, so `protected_packages`
and OS-essential never see it. It leaves no history entry. **A user cannot discover it before it
happens, cannot see it while it happens, and cannot find it afterwards.** Every guarantee in
II.10 is expressed as a refusal, and a refusal that is never consulted is not a weak
protection — it is decoration.

**What makes it worse than the sum of its parts.** It is armed by interruption. Any run that
dies mid-install — Ctrl-C, a reboot, a killed process, S26's hour-long sleep hitting a timeout —
writes a record that the *next* run recovers by uninstalling. So the natural response to a hung
Shall is precisely the action that loads the gun, and the person most likely to fire it is
someone debugging Shall. That is how a rare fault becomes a reproducible one.

**What it says about this document.** II.10 has claimed "every removal path calls it" through
thirteen sessions and an adversarial audit that existed specifically to find false claims. It
was false the whole time. **S6 looked directly at `heal` and asked the guard question — of the
other branch**, wrote down a careful answer, and moved on; the branch it did not read was four
lines away. A claim that is checked against seven paths and not the eighth reads exactly like a
claim that is true. **The audit habit this document is proud of did not catch this. Running the
thing did.**

**The number that should end any argument about test coverage: `cargo test` is green.** 970
passed, 0 failed, measured on this tree on 2026-07-23 — over a binary that uninstalls declared,
protected software with no guard and no record. The suite is not weak; it is aimed at the paths
somebody thought of, and nobody thought of this one. **IV.1's rule that a check must be able to
fail is satisfied here and still bought nothing**, because no check was pointed at `heal`'s
install branch at all. A test that does not exist cannot be vacuous — it is just absent, and
absence is invisible in a green run.

Filed in full below as **S24**, with **S25**, **S26** and **S27** — the three that stood between
anyone and finding it.

## VI.1 Killed by this design — no work needed

- `-g` replaced the wish list while the ownership record never moved → phantom drift → the
  purge. **(C2, C4, C5, C6, C14, D1, D2)**
- `keep.txt` read as a wish list. **(E2, E10)**
- Undefined manifest read order. **(C4)**
- `upgrade --profile` matches zero packages, always — builds `manifest:work`, tags are
  `manifest:work.txt`, and per-profile provenance is destroyed at materialize time anyway.
  **(verified)**
- `-vim` in a manifest becomes a package named `-vim` while the README documents it as
  exclusion. **(verified)**
- Generations record absolute paths → rollback writes to a dead directory and reports
  success. **(verified)**
- Generation ids are epoch seconds → two syncs in one second silently overwrite history.
  **(verified)**
- `config.toml` never travels with history.
- "Rollback" means two things depending on flags; `undo` assigns the registry by fiat.
- Manifests stored twice on independent budgets.
- ~~Two snapshot retention engines. **(E4)**~~ **DONE 2026-07-17** — the duplicate
  `prune_stale_snapshots` is deleted; all pruning goes through `prune_with_policy` +
  `Config::snapshot_retention()`. See the Phase 4 "one retention engine" note.
- Every bundle restore is unverified by construction. **(H1)**
- `shall repo add` records nothing → modules are not portable.
- ~~`shall shim --source` is required, documented, and thrown away. **(verified)**~~ **DONE
  2026-08-09 (`Y18`.3, ruled the same day).** The command was gone — II.16 records it becoming
  the line `shim:jq@source=cargo:jq` — but **the bug had only moved house**: `source` stayed a
  legal option that nothing read, so a line naming a provider still got whichever provider the
  bare name happened to resolve to. The shim now reads `@source=` off its own declaration at run
  time and provisions and execs that; there is no sidecar store, because the config that declared
  the shim is the one the shim process already loads. II.17 gained the missing register row in
  the same change. Found by `named_commands_exist_tests` once it was pointed at `docs/`, which is
  the second thing this entry is evidence for: **a defect whose subject is deleted is not a
  defect that is fixed.**
- Holds (machine-local) silently beat signed locks (portable).
- `@lease=` in a manifest is inert. `remove --temp` is undone by the next sync.
- `orphans` message false on both counts. **(C12)**
- "prune" means four things. **(E3)** "orphans" means two plus a dead third. **(E5)**
- `confirm_destructive` gates the wrong thing. **(E12)**
- Any typo becomes a package name.
- `ScopedFilter::None` — an implicit spare-everything switch.

## VI.2 Carried forward — still real work

| | |
|---|---|
| **C1** | ~~`uninstall` never consults protection. `shall uninstall libc6` proceeds~~ **DONE by architecture, verified 2026-07-17.** `handle_uninstall` undeclares then calls `handle_sync`; the removal is drift, and `SyncEngine::sync` runs `guard::enforce` (`GuardScope::Sync`) before touching anything — so `uninstall libc6` is refused (libc6 is protected). No direct-removal path bypasses sync. → **Phase 3** |
| **C3** | ~~lease and `absent:` removals skip protection~~ **DONE, verified 2026-07-17.** Leases were deleted in Phase 2 (no lease removal path exists); `absent:` becomes drift removed by `sync`, which is guarded. → **Phase 3** |
| **C8** | ~~ghost-shell exit force-removes with no protection, no guard, no confirmation~~ **DONE, verified 2026-07-17.** `cleanup_transient_env` builds a Remove graph and routes it through `engine.sync(changes, GuardScope::ShellExit)`, which runs `guard::enforce` first — the exit removal is guarded like every other. → **Phase 3** |
| **C9** | ~~lease expiry implemented twice with different semantics~~ **DONE, verified 2026-07-17.** Owner decision: the intended end state is **`@expires=<absolute date>` temporary installs reclaimed by one sweep** (the `@lease=2h` syntax was retired in Phase 2). There is now exactly **one** implementation: `App::sweep_expired_leases` (`context.rs:653`), driven by the single `StateRegistry::get_expired_packages` (`state.rs:290`), called once from `perform_maintenance`. The second, differently-behaved expiry path went with the deleted lease command. The sweep running on every state-changing command is correct — it is how a due expiry is honoured promptly. → **Phase 3** |
| **C13** | **DONE (Phase 2r/2s).** The grammar is the one **statement** parser, and every non-validating `backend:name` splitter is gone. `config/manifest.rs` (`ManifestEngine`) was deleted whole in Phase 2n; `app/insight.rs`'s splitter is gone; `main.rs`'s last one (`lease set`) went with the retired `lease` command in 2s. **Re-measured 2026-07-26 — the splitters that remain are none of them the C13 risk** (the 2026-07-17 line numbers below had all moved, which is why this row now names what each site *does* rather than only where it is): `config/parser.rs:226` (`split_removal_target`) consults the registry before treating a prefix as a backend; `app/context.rs:1336` splits a `repo:` key **Shall itself wrote** into the extras ledger and calls `registry.get(backend)` on the next line, erroring by name if it is absent; `core/extras_lock.rs:62` splits another ledger key, never a user-authored line; `model/resolve.rs:964` extracts the *name* half of an already-resolved key; `parsers/ecosystem.rs:298` splits `name:ver`, not a backend. The `main.rs` `requires` splitter is gone entirely. None reads an unvalidated backend name. → **Phase 2** (done) |
| **B3** | `unprotected_packages` doesn't beat OS-essential; `shall protected` reports the opposite of what the guard does → **Phase 3** |
| **E7** | Adopt consulted `protected_packages`, so "protected" meant *never remove* in the guard and *never adopt* in `migrate.rs` — a package you could not adopt and could not remove, for the same reason. **FIXED (Phase 2m):** adopt takes every manual package, protected ones included (protection stops the removal, not the adoption — V.26); only OS-essential is held back, in the commented-out second section II.9 specifies → **Phase 2** (fixed) |
| **E6** | "unmanaged" had two implementations that disagreed. **FIXED (Phase 2m).** `unmanaged` (II.8) is now *"what `adopt` would adopt"* — one crawl, `discover().adopt`, ~103 on a stock Ubuntu. The other question — every installed package Shall does not manage, dependency closure and all, ~476 — is `installed_but_unmanaged()`, wanted only by `purge-unmanaged` (II.11). Same word, two questions, one answer each → **Phase 2** (fixed) |
| **E11** | ~~suspension restore implemented twice~~ **DONE 2026-07-17** — `sweep_due_suspensions` (timed) and `restore_session_suspensions` (shell-exit) carried byte-identical reinstall-and-clear loops; extracted one `App::restore_suspensions(items, occasion)` both call (`occasion` was the only difference, and only in log wording). `cargo check --lib` clean. → was **Phase 3** |
| **F1** | `network_timeout_secs` lies (`.max(10)` floor); `max_parallel` detected; `priority` reason in a comment → **Phase 5** |
| **F3** | ~884 marketing comments + **32 false ones** → **Phase 0**. The rule → `CLAUDE.md` → **Phase 5**. **SWEPT 2026-07-26, and the residual is stated rather than claimed.** The 884 was counted on 2026-07-16 against a much smaller tree; `src/` carries **9,593 comment lines** today. Three classes were defined, greped exhaustively and cleared: **marketing/self-praise** (the sales vocabulary finds nothing; every `cleanly`/`for free`/`happily` hit is a constraint, e.g. "a `BTreeMap` so the file diffs cleanly in git"); **citation-as-explanation** — a comment whose whole content is a spec reference — went from 13 to 4, and the 4 that remain are the closing parenthetical of a sentence that states the constraint, which is what `CLAUDE.md` asks for; and **restatement** — a comment that respells the identifier beneath it — of 64 flagged by identifier-overlap, 10 were real and are gone, the rest earn their place (`cli/args.rs` doc comments **are** the `--help` text, not code comments). **What is not claimed:** a comment that narrates its block without echoing an identifier is a per-comment judgement over 9,593 lines, and one pass cannot honestly call that finished. The rule is in `CLAUDE.md` (P6) and new code follows it. The 32 false ones are separately closed (F5). |
| **F4** | ~~33 vs 50 backends. **(measured: 41 registration sites)** Compute it~~ → **Phase 5. 2026-07-19: all three typed counts deleted (`args.rs`, `lib.rs`, README), so nothing is left to rot; the generated count lives in `shall check health` ("of N total", `verbs/check.rs:1012`), which already builds the registry. `--help` querying the registry was NOT built — see F4's Phase 5 entry for why. **CLOSED — owner confirmed 2026-07-26: leave as-is, do not wire `--help` to the registry** (it would make help read config from disk and give it a way to fail; `check health` already carries the live count). Nothing further to build.** **Ruling stands; its justification was re-pointed 2026-08-07** — it named `shall doctor`, which S38 folded into `check <section>`, so a CLOSED entry rested on a command the program does not have. The second reason (help must not read config) never depended on it. Found by `named_commands_exist_tests` once it was pointed at `docs/`. |
| **F5** | ~~false doc comments~~ **DONE 2026-07-19.** Four of the six were already true at `HEAD` (two because the code moved to meet the comment, one because v7's `sync` removed the asymmetry it failed to disclose); the two fixed here were `sweep_expired_leases`, which documented itself with a `@lease=` key the grammar rejects and a `prune` command that no longer exists, and three surviving "ghost shell" references → **Phase 5** |
| **G2** | 104 soft assertions — **located 2026-07-17**: they are the 102 `soft "…"` calls in the **shell** harness `docker/integration/run-in-container.sh` (NOT Rust). That harness is obsolete (built on deleted `-g`/`generation`/`lease`), so G2 folds into "rebuild the harness" — soft→hard is part of that rewrite. → **Phase 5** |
| **G3** | ~~teleport, adopt, shim, cockpit, undo unverified~~ **DONE.** shim/adopt/undo have tests, and `cockpit` is now `history` (still tested). ~~The last gap closed itself: `teleport` was deleted outright in R2 (2026-07-19), so its untested branches are not untested code, they are no code.~~ **That sentence is stale, and the correction is the interesting half (found by the harness, 2026-07-24).** R2 deleted the imperative `Teleporter` **engine** — the one that built its own transaction and bypassed the guard — and the history recorded "grep is silent". It is not silent: a later session **re-added `teleport` as a declarative command**, and correctly. `handle_teleport` (`main.rs:1485`) calls `App::retarget` to rewrite the declaration's line and then `handle_sync`, which runs the guard — exactly the shape R2's own ruling permitted (*"a convenience verb must route through `handle_sync`, never its own transaction"*). It is in II.8's table. **No second transaction engine exists; `core/transaction.rs` is the only one.** The lesson is the one this file keeps re-learning from the other direction: *a deletion recorded as permanent was undone by a later session, and the record of the deletion outlived the deletion.* → **Phase 5** |
| **H2** | `sync/mod.rs:463`, `shell/mod.rs:126` → **Phase 5** |
| **S1** | ~~`reconcile_shims` is never called~~ — **this was false.** `sync` calls `reconcile_all_shims` on every successful run, which calls `remove_shim` for every managed package that is not shimmed. `remove_shim` deleted `~/.local/bin/<name>` by filename alone, with no check that Shall created it — so a managed package named `jq` made every sync delete the user's own `~/.local/bin/jq`. `~/.local/bin` is shared. **Fixed in Phase 0f**: ownership is now tested (a shim is the shall binary under another name — same file as `current_exe`, or a byte-identical copy). Three regression tests added; they could not exist before because `bin_dir` was private with no injection point, which is why G3 lists shims as unverified. |
| — | `bundle` has no restore code and no end-to-end test → **Phase 4** |
| — | air-gap artifacts need the whole dependency tree, and most backends can't → **Phase 4** |

### Found during implementation

Each verified against the code at the time it was found, with the evidence in the commit
that recorded it. Assigned to the phase that owns the mechanism, not the phase that found it.

| | |
|---|---|
| **S2** | ~~**Age-based snapshot retention is dead for btrfs and ZFS.** Both providers' `list()` hardcode `timestamp: Utc::now()`, so `Snapshot::parse_time()` always returns *now* and every snapshot reads as zero seconds old.~~ **DONE (data-feeding fix), 2026-07-17.** Both `list()` methods now derive the real creation time from the id Shall embeds when it creates a snapshot (`shall_pre_<label>_<YYYYMMDDHHMMSS>` / `<dataset>@shall_<YYYYMMDD_HHMMSS>`) via the new pure `Snapshot::time_from_id` — no dependency on btrfs/zfs reporting creation time, which varies by version. `retention.rs` was already correct, so `max_age_days`/`keep_days` now fire. **What I checked:** `cargo build --all-targets` clean; **5 unit tests written but NOT executed this session** (no-run constraint) — btrfs/zfs id round-trips (timezone-independent), age ordering, `None` for un-embedded ids, and the one-assertion regression (a week-old snapshot reads as ~7 days, not ~0). **Not covered:** an actual btrfs/zfs machine (untestable here) — but the risky part (the parse) is pure and tested. **Remaining Phase 4:** "one retention engine" (unifying the retention call paths) is a separate item; this fixes the data those paths consume. |
| **S3** | ~~**Snapshot retention never prunes Windows restore points.** The ownership filter is `id.contains("shall")`, but `WindowsRestoreProvider` sets `id` to a bare `SequenceNumber` (`"12"`) and puts the `Shall:` marker in `description`.~~ **DONE, 2026-07-17.** New `Snapshot::is_shall_owned()` checks **both** id and description, case-insensitively (so `Shall:` matches as well as `shall_`); the retention filter uses it instead of `s.id.contains("shall")`. Now the only such ownership check in `src/` (grep-verified). **What I checked:** `cargo build --all-targets` clean; **2 unit tests written but NOT run this session** — btrfs/zfs/Windows all recognized as owned, a Windows Update / manual snapshot left alone. |
| **S4** | ~~**`create_shim` overwrites any existing file at the target without asking.** Deliberate for redeploying a shim, but it silently destroys a same-named binary the user owns in `~/.local/bin`. `remove_shim` was fixed in Phase 0f to test ownership; the create path still does not.~~ **DONE, 2026-07-17.** `create_shim` now runs the same `is_deployed_shim` ownership check `remove_shim` uses before overwriting: a redeploy of Shall's own shim proceeds, but an unmanaged same-named file is **refused** (`Error::Validation`), not clobbered. In auto-reconcile (every sync) the refusal is a non-fatal warning and the file is preserved; an explicit `shall shim` request propagates the error. **What I checked:** `cargo build --all-targets` clean; **1 unit test written but NOT run this session** — a user's `~/.local/bin/jq` survives `create_shim("jq")` unmodified and the call errors. Verified the two call paths (`reconcile_all_shims` warns; `context.rs` propagates) by reading them. |
| **S5** | ~~**`--progress` is a lie.** `#[arg(long, global = true, default_value = "true")]` on a `bool` — clap derives `ArgAction::SetTrue`, so it is always true and there is no way to turn it off.~~ **DONE, 2026-07-17.** It was also **dead** — `cli.progress` was read nowhere. Replaced with a plain `--no-progress` flag (a real `SetTrue` off-switch) wired in `load_and_merge_config` to set `config.show_progress = false`, which `create_progress_reporter` already consumes (`context.rs:82`, `services.rs:105`). **What I checked:** `cargo build --lib` and `--bin shall` both clean. `cargo build --all-targets` hit an **environment OOM** (os error 1455, "paging file too small") while linking a test binary — not a code error; lib+bin prove the code compiles. No unit test (it's a clap-wiring + config-merge change; the effect is `show_progress` flowing to the reporter). |
| **S6** | **Reporting DONE, 2026-07-17.** `heal()` no longer recovers silently: each recovered action is logged at **info** (`reinstalled apt:nginx (completing an interrupted install)`), and it ends with a one-line summary (`recovered N operation(s): …`) plus a `warn` list of any it could not recover — visible without `--verbose`. Successes were previously `debug!`, i.e. invisible, which is exactly P3's "didn't work and said nothing". `cargo check --lib` clean; no unit test (log-output change). **Deliberately NOT done — guard-on-heal-removal:** the decision text says a heal removal should route through the guard like any other, but heal *completes an operation the user already initiated and the WAL already recorded*; gating it on the guard means a protected/essential package caught mid-transaction could become **un-healable**, leaving the WAL permanently inconsistent. That trade-off is a real decision for the owner, not a blind wiring job, so it is flagged here rather than done. The original decision, unchanged: **`sync` heals without asking. DECIDED 2026-07-16: it should, and that is what declarative means.** A half-finished transaction is a state nobody declared — it is drift, and removing drift is sync's job → **Phase 3** |
| **S7** | ~~**A crash left unhealed for 4 hours becomes unhealable.** `Journal::cleanup` reclassifies stale `InProgress` entries to `Abandoned`, and `get_incomplete_actions` (what `heal` acts on) excludes `Abandoned`. So the window to recover a crashed transaction silently closes.~~ **DONE 2026-07-19 — this is the same defect R23 found from the other end**, and neither entry knew about the other: S7 saw the window closing, R23 saw the node it strands. `get_incomplete_actions` and `needs_recovery` now include `Abandoned`; covered by `an_aged_out_crash_is_still_healable`. **The 4h magic number remains a magic number (P5)** — it is now only a *reclassification* threshold rather than a deadline, so it no longer decides whether recovery happens, but nothing states why it is four hours. → **Phase 5** |
| **S8** | **DONE, 2026-07-17.** `FORBIDDEN_PATHS` renamed to `REGISTRY_READ_FORBIDDEN_PATHS` with a comment saying it guards the snapshot-**registry-read** (diff) step only — the old name claimed a global "never touch" ban that `execute_restore` (which rolls all of `/` back) plainly violates. The refusal message now says it is refusing to *read a registry* from that path (a would-be arbitrary-file-read), not that the path is globally off-limits. And the restore confirmation now states plainly that it reverts the **entire filesystem** — every file, configs and data included, not just the packages in the summary — before asking for `RESTORE`. `undo` and the check are both kept, per the decision. `cargo check --lib` clean; no unit test (message/comment/rename change). *(Original decision, for context:)* **`undo` lies about scope; there is no safety hole. DECIDED 2026-07-16.** What `undo` does: list filesystem snapshots, mount the chosen one read-only, read the `registry.json` *inside* it, diff that against now, show a package-level summary, and on confirmation hand the snapshot to btrfs/timeshift to restore. `FORBIDDEN_PATHS` guards step 3 only — which directory `undo` will read a registry out of, so a crafted path cannot make it parse `/etc/shadow` as JSON. That is a real check doing a real job. Its *name and comment* claim "paths NEVER allowed to be accessed", and restore goes over `/` including all of them. So the defect is the false claim, not the check. **Keep** the check (renamed to say it guards the snapshot-read path); **delete** the global claim; **keep** `undo` (nothing else turns a snapshot into a package diff), but restore must state plainly that it rolls back the entire filesystem before asking. Gating restore on the list would refuse every root snapshot, i.e. delete `undo` by accident → **Phase 3** |
| **S9** | ~~`remove_package_from_local` (`parser.rs:290`) matches a bare target against the BACKEND prefix~~ — **FIXED in Phase 2e, and this row was stale in three ways (2026-07-17).** The function is gone (`grep` empty). The removal path is now `model/edit.rs:378` `matches()`, which parses each line **through the grammar** and compares `d.selector`, never the prefix; regression test at `edit.rs:669` (`npm:typescript` survives, `apt:npm` dies). **It did not "die with `local.txt`" — it died of `edit.rs`, and `local.txt` still has readers** (`insight.rs:418`). ~~→ **Phase 2**~~ **Nothing owed.** *(Both surviving prefix-splitters were checked for this defect shape and do not have it — `insight.rs:429` requires both halves, `manifest.rs:90` matches the name half.)* |
| **S10** | **`cargo test` wrote to the developer's REAL data dir**, and one bad file bricks every command. `TestKernel` (named `shall_hermetic_`) isolated `registry.json`, groups and tmp, but `Journal::new()` hardcoded `safe_data_dir()` — found at 733KB of test noise in `%APPDATA%/shall/journal.json`. Fixed in Phase 2b by injection (`Journal::at`). ~~**The remaining half is real:** `Journal::load_sync` errors on a bad parse -> `App::new` fails -> EVERY command fails, with no message saying which file to delete or how to recover.~~ **DONE, 2026-07-17.** `load_sync` no longer returns `Err` on a corrupt WAL: it moves the bad file aside to `<path>.corrupt` (preserved for inspection, and so it stops re-triggering), `warn`s loudly with the path and that an interrupted op can't be auto-recovered (re-run `sync`), and starts fresh — so `list`/`plan`/everything still run. Failing loud AND leaving a way out (P3). **What I checked:** `cargo check --lib` clean; **2 unit tests written but NOT run this session** — a corrupt WAL constructs successfully + gets moved to `.corrupt` off the live path; a missing WAL starts fresh. → was **Phase 5** |
| **S12** | **DONE (Phase 2o + 2p). All the extras now have somewhere to go.** They land in `DesiredState::extras`, which the old `resolve_desired_state` dropped because the seam carries `.packages` only; `sync` resolves the whole `DesiredState` now and applies the extras in II.7's ordering. **Phase 1 — `App::apply_repositories`:** `repo:` lines FIRST (add repo, refresh index) before the package plan, each repo naming its backend (V.47), a backend not in `priority` refused in the file (V.15). **Phase 3 — `App::apply_dependents`:** `shim:`, `service:`, `link:` AFTER the package plan executes — a shim wraps a tool that must already be installed, a service enables a unit a package just laid down, a link writes a config a package expects, so they are the *dependent* phase and cannot be interleaved with packages. Applied in declaration order (a config `link:` above the `service:` that reads it keeps that order). A dependents-only config (no package changes) still runs the phase — the "System matches" exit checks `has_dependents()`. Verified against the binary: `schedule:` warns (file:line), Flight plan installs the package, THEN service → link → shim preview. ~~**Only `schedule:` is still unwired** — the scheduler owns it, not `sync` — and it is now the one line the resolver warns about.~~ **(Stale as of 2026-07-20: the resolver reads `schedules` at `resolve.rs:303-305` and that warning was deleted. S21 wired it on 2026-07-17.)** `watch` (the unattended `sync`) runs the same three phases now, so an unattended reconcile also adds repos and applies dependents. The **teardown** direction — reconciling away a *removed* extra — is tracked separately as **S20**. → **Phase 2** (forward direction done)
| **S20** | **DONE, 2026-07-17.** The applied-extras ledger is built. New pure `core/extras_lock.rs`: `ExtrasLedger` (→ `locks/extras.toml`, an ordered `BTreeSet` of extra keys), `extra_key(&Statement)` (`repo:apt:ppa:x/y`, `service:nginx`, `shim:rg`, `link:…`, `schedule:…`), `split_key`, and `drift(declared)` = recorded − declared. New `App::reconcile_extras` diffs the currently-declared extras against the recorded set, undoes the difference via each backend's existing removal path (`shim`→`remove_shim` (ownership-safe), `service`/`link`→`as_installable().remove`, `repo`→`as_repo_manager().remove_repo`, `schedule`→new config-free `SchedulerManager::deprovision`), then records the new set. Best-effort per item (a backend that can't undo one warns and the rest continue); dry-run previews and never writes; a no-op sync neither works nor rewrites the ledger. Wired as sync phase 5 in `handle_sync` (incl. the "System matches" early-exit, so **removing the last extra line still triggers the undo** — the exact bug) and `watch_reconcile`. **What I checked:** `cargo check --lib`/`--bin shall` clean; **6 unit tests written but NOT run this session** (key stability + parse, package-has-no-key, drift = recorded−declared, no drift when unchanged, a newly-declared extra isn't drift, TOML round-trip, missing-file-loads-empty). **Untested here:** the actual OS undo (systemctl/repo tools — none on this box); the ledger + diff + dispatch structure is what's covered. → was **Phase 4** |
| **S21** | **DONE, 2026-07-17.** All three parts wired: **(1)** the resolver now reads the `schedules` file in `statements()` via `parse_document` + `statements_for(facts)`, `when`-gated exactly like a module (absent file → no schedules). **(2)** New `App::apply_schedules` maps each line to a `ScheduleConfig` (new pure `model/schedule.rs::schedule_config`, which validates `cron`+`run` are present and rejects unknown keys) and provisions it through the new config-free `SchedulerManager::provision` (extracted `validate_cron` from `add_schedule`, no more duplicated cron logic); wired as II.7 phase 4 in `handle_sync` (dry-run aware) and `watch_reconcile`, and the "nothing to do" exit now accounts for schedules. **(3)** The file-context rule lives in `collect`: a `Statement::Schedule` whose origin is not the `schedules` file is an error naming the line and pointing at the right file. **What I checked:** `cargo check --lib` and `--bin shall` clean; **6 unit tests written but NOT run this session** (mapping happy-path, optional `notify`, missing `cron`/`run` errors, empty-cron-as-missing, unknown-key refusal). **Untested here:** the actual OS provisioning (systemd/launchd/Task Scheduler — no such runner on this box); only the line→config mapping, the file read, and the file-context rule are unit-covered. **Not done:** `init` does not scaffold an empty `schedules` file (not required — the resolver treats absent as "no schedules"). Was → Phase 5. |
| **S13** | **A bare name and an explicit one were two packages, not one.** `model::resolve` keys the merge on `backend:name`, and a bare `ripgrep` is keyed `?:ripgrep` until something probes it — so `ripgrep` in one module and `cargo:ripgrep` in another never met, never reconciled, and both reached the planner. Found while wiring the seam and **fixed there**: `Resolver::statements()` and `Resolver::collect()` are now separate, the caller probes in between, and `with_bare` hands the answers back so the merge sees real backends. II.7 rule 5 was silently not applying to every bare line → **Phase 2** (fixed) |
| **S19** | **`@lease=2h` still worked by hand, and it was the one option key that could uninstall your package. FIXED.** II.16 retired it — nothing Shall writes used it — but `StateRegistry::add` still read `options["lease"]` and turned it into a real `expires_at`, and **the grammar validated no option keys at all**, so a hand-written `apt:jq@lease=2h` was silently a package that uninstalls itself, on the `sweep_expired_leases` path C3 says bypasses the guard. Both halves closed: **II.2's key table is now enforced by the grammar** (an unknown key is an error naming the file and line, and `@lease` gets a hint pointing at `@expires=<absolute>`), and `state.rs` no longer reads `lease`/`duration`. **This was Phase 1's job** — "unit tests for every grammar rule above, including every error case" — and II.2's table was the one rule with no test → **Phase 1** (fixed in 2l) |
| **S18** | **`auto_lock_checksums` rewrote your module files on every sync. FIXED (Phase 2n) by deletion.** It spliced `@sha256=…` into the line you wrote — II.16 says Shall must not rewrite your files, and a checksum is a generated fact, which II.6 keeps in `locks/`. The whole `attempt_auto_lock` path is gone, and with it `ManifestEngine` (its last caller): a second file-editor with its own `split_once(':')` parser (C13), `load_locks`/`update_lock`/`manifest_files` all already dead. `groups_dir` refs 77 → 64. **The supply-chain intent survives, unbuilt:** recording an artifact's hash so a changed artifact is caught (II.12) belongs in `locks/<backend>.toml` → **Phase 4** (locks and git) |
| **S16** | **`--allow-mass-removal` deleted protected packages. FIXED.** `guard::enforce` returned `Ok(())` for *every* objection once the flag was set, so the flag meaning "yes, 50 packages is what I meant" also carried `python3` through. II.10 is explicit — `max_removals` exceeded → "cannot skip, `--allow-mass-removal`"; protected / OS-essential → **"nothing overrides"**. A confirmation asks; a refusal says no (V.26). The flag now clears only the count objection. **There was a test asserting the old behaviour** (`enforce_refuses_without_opt_in_and_proceeds_with_it`, which asserted the flag lets `python3` through) — the bug was written down as an expectation, which is why nothing caught it → **Phase 3** (fixed) |
| **S17** | **`[guard.enforce_on]` was a config key that switched the guard off, per command. DELETED.** Ten booleans — `apply`, `prune`, `sync`, `watch`, `upgrade`, `rollback`, `canary`, `remove`, `shell-exit`, `leases` — each of which made that command able to remove **anything, without limit**, protected and OS-essential included. It is not one of II.10's refusals; it is a switch that turns off all of them. V.21 says **no setting anyone can flip, inherit, or copy from a dotfiles repo** makes a routine sync delete something it did not install, and this was exactly that setting. The config template documented it, and `shall protected` printed which commands were unguarded. All gone → **Phase 3** (fixed) |
| **S15** | **`install` and `uninstall` both had P1 backwards. FIXED for both (`install` 2g, `uninstall` 2l).** P1 says an imperative command *is* a shortcut for editing a file and syncing, so the edit is the operation and the install is what convergence then does about it. Backwards, every refusal on the write landed *after* the package was on the machine. `install` is now `declare` -> `sync` (behind the guard for the first time; `--temp 2h` writes `@expires=<absolute>`). **`uninstall` is now `undeclare` -> `sync` too** (`main.rs:1182` edits the file, `:1190` calls `handle_sync`), so removal goes through the guard, the plan and the counts like any other drift — the symmetric pair V.39 describes. **Verified adversarially 2026-07-17: the old "still inverted, blocked on `prune_on_sync` default false" claim was stale in both halves** — `prune_on_sync`/`with_prune` are deleted (Phase 2h; only comments remain), and sync removes drift by definition (V.34). `uninstall --temp` is likewise done: it writes `absent:...@until=` (II.16). → **Phase 2** (done) |

| **S14** | **FIXED (Phase 2w).** `shall init` filled `priority` from `registry.available()`, which includes the pseudo-backends `service`, `link`, `web`, `github` — teaching a new user that the file answering *"which package managers, in what order"* contains four things that cannot resolve a package. `starter_order` now drops `service` and `link` (dependent statements, never priority-gated, never resolving a bare name). **`web`/`github`/`appimage` stay on purpose:** the model refuses an explicit `web:…` unless `web` is listed (V.15), so excluding them would break those specs — **the original S14 was imprecise to lump them in.** Test: `service_and_link_are_not_listed_but_artifact_backends_are`. *(The deeper "is this a package manager" capability probe the row imagined proved unnecessary: the two truly-not-managers are a fixed pair, and the rest legitimately need listing.)* → **Phase 2** (done) |
| **S11** | ~~**The test harness is not hermetic by construction, only by remembering.**~~ **DONE 2026-07-19, and the row was half-stale in a way that proves its own point.** `mock_providers::TestKernel` had already been fixed to isolate both roots, with a test asserting it — but **`tests/exhaustive_backend_suite.rs` is a SECOND fixture, and it set `config_root` and `tmp_dir` and forgot `data_root`**, so every run of that suite wrote the registry, journal and snapshots into the developer's real data dir. The fix that was applied in one place was never applied in the other, which is exactly "isolation that depends on remembering" — the row was fixed and filed as open, while the thing it described was still happening one file over. Now structural: `Config::sandboxed(&Path)` sets **every** path (config root, data root, tmp, github, web, appimages) in one place, both fixtures call it, `grep -rn "Config::default()" tests/` is empty of fixtures, and `sandboxed_puts_every_path_under_the_sandbox` asserts all six rather than the two obvious ones. → **Phase 5** (done) |

| **T1** | **FIXED 2026-07-23.** Decrypt mode no longer calls `backup_once` — it compares the target's contents itself and writes through the new `write_secret`, so the branch cannot reach a backup at all rather than being trusted not to. Scoped to mode D exactly as the sibling check asked: inline-content and template modes still back up, and T6's ruling now makes that backup a rollback instead of a leak. Test asserts the absence **by path**, because what the bug produced was a file sitting there. Original entry follows.<br><br>**`link:`'s decrypt mode leaks the previous secret in plaintext.** `apply_managed_content` calls `backup_once` (`link.rs:154`, `:319`) for every managed write, mode D included — so when the target already holds a secret, Shall copies it to `<target>.shall-backup` under the ordinary umask before writing the new one. The `0600` at `:285` is applied to the target and to nothing else, and no ignore file mentions `.shall-backup`. Found while documenting mode D for the readme; the code is shipped and the leak is live. *Recommendation:* mode D never backs up — `backup_once` exists so a user is not silently robbed of a config file they hand-wrote, and a secret Shall itself decrypted a moment ago is not that. **Sibling check owed with the fix:** the template and inline-content modes share `apply_managed_content`, so the change must be scoped to the decrypt path and not to all three. See **XII.5**. → **Part XII** |
| **S24** | **FIXED 2026-07-23 — see VI.0. The pre-reinstall `remove` is deleted** (`sync/mod.rs`), and with it the comment at `:404-408` that argued for it. Four tests in `tests/critical_paths_tests.rs` hold the shape rather than the symptom: recovery of an interrupted install issues no `uninstall` **and** re-runs the install; an interrupted *removal* still removes (so a fix cannot cure one branch by disabling the other); a protected package's interrupted removal is still refused and the entry still resolves; and a dry run performs none of it. **The test that vouched for the bug is gone**: `test_journal_self_healing_logic` primed the mock with `brew uninstall stale-pkg` and was documented as verifying that healing "correctly uninstalls" — its duplicate in `tests/security_and_resiliency_tests.rs` (`test_journal_wal_healing_logic`, same fixture, weaker assertions) went with it rather than being ported. **The fixture lied too, and that is the sibling worth recording**: `TestKernel` set `config.dry_run = true` while every test using it asserted that mutations happened, so no test using it could ever have caught a preview-only defect. It is now a real run against a mock executor. **Not built, deliberately**: the per-backend remove-before-install capability II.10 describes has no backend that needs it, and a capability with no caller is a second path with no test. The history below is the original entry.<br><br>**OPEN, and it uninstalled software on the owner's machine (2026-07-23). Recovering an interrupted *install* uninstalls the package first, past the guard, past the plan, past the counts and past the history.** `sync/mod.rs:432`: when `heal()` finds an incomplete `Install`, it runs `let _ = handler.remove(&package)` before reinstalling, so a package that is **declared, wanted and already present** is uninstalled by a command that was asked to install something else. Three defects in the one line: it **never reaches the guard** (the guard call above it at `:402-427` covers the interrupted-*removal* branch only), its result is **discarded** (`let _`), so a successful remove followed by a failed reinstall leaves the package gone with no error, and the removal is invisible to the plan, the counts and `--dry-run`. **Observed, not theorised:** `shall -y install nimble:nimjson` spawned `winget uninstall --silent Google.Chrome.EXE`; the journal at `shall-it-win-state/journal.json` carries the matching incomplete `Install` of `winget:Google.Chrome.EXE` from `modules/adopted.txt:82`. **S6 recorded the guard question for the removal branch and explicitly deferred it; nobody looked at the install branch**, which is the worse of the two — a heal removal completes something the user started, this one starts something nobody asked for. **RULED (owner, 2026-07-23): `heal` does not remove at all.** The recommendation offered two halves and the owner took the stronger one: the pre-reinstall `remove` is **deleted**, not guarded. Recovering an interrupted install re-runs the install, which every manager Shall drives can do over a half-installed state. **Remove-before-install becomes a per-backend capability, off by default**, and where a backend declares it the removal is an ordinary removal — guard, plan, count, `--dry-run`, and an error on failure instead of `let _`. Routing it through the guard instead would have left a delete on the path nobody watches and trusted the check to catch it, which is the arrangement that just failed for thirteen sessions. **Removing the delete removes the class.** Rule in II.10, reason in V.64. **Family owed with the fix:** every `let _ = ` around a removal, and the `heal` path in `watch`. → **Phase 3** |
| **S25** | **FIXED 2026-07-23, in the same change as S24.** Both halves: `heal()` returns after reporting what it would recover when `config.dry_run` is set — **the check is inside `heal()`, not at the call site the ruling named**, because `shall heal --dry-run` reaches the same function by another route and moving the call in `reconcile` would have fixed one caller and left the other, which is this document's most-repeated failure; and `acquire_data_lock` no longer exempts `--dry-run`, so the exemption is `READ_ONLY_COMMANDS` and nothing else. A preview of a writer now waits for the writer, which is the point: it reads state the writer is rewriting. Test: `a_dry_run_reports_the_recovery_and_performs_none_of_it` asserts an empty call log **and** that the journal still wants recovery afterwards — a preview that resolved the entries would leave the real run with nothing to do. The original entry follows.<br><br>**`--dry-run` mutates the machine, and does it without the lock.** `main.rs:343` returns no `DataLock` when `cli.dry_run` is set, and `reconcile` calls `engine.heal()` at `main.rs:475-478` — *before* the `if app.config.dry_run` branch at `:540` whose comment reads "Dry-run is preview-only: never prompt, never mutate". So a dry run enters S24's remove-and-reinstall, unserialised against whatever real run is in flight. **Observed:** `shall --dry-run sync` printed `recovering 3 interrupted operation(s) from a previous run` and then stalled inside the first one on S26's rate-limit sleep — **so the path was entered and nothing was reached to remove, which is luck, not a design.** The journal is unchanged, which is the evidence: recovery had not got as far as acting. **RULED (owner, 2026-07-23): as recommended, and it lands in the same change as S24.** `heal()` moves below the dry-run branch and reports what it would recover; the lock exemption then covers readers only, which is what `READ_ONLY_COMMANDS` already says it is for. One code path, one fix — S24 and S25 are the same defect seen from the execute side and the preview side. → **Phase 3** |
| **S26** | **FIXED 2026-07-23.** Capped, retried once, settable — `rate_limit_max_wait_secs` in `preferences.toml`, default 30. Past the cap it is an error naming the host and the seconds until reset, and saying which key to raise. **The retry is the half the entry did not name and the reason the old wait was pure loss**: the handler slept and then returned the same 403 it had slept on. The decision is now a pure function (`rate_limit_action(status, header, now, cap)`) with five tests — an integration test cannot make api.github.com rate-limit on demand, so the part that has to be right is the part that is testable. **A 403 that is not a rate limit is never waited on at all** (a bad token, a private repo, a header that does not parse, a limit that already reset): waiting changes none of them, and the old code would have slept on any 403 carrying a future timestamp. Original entry follows.<br><br>**The GitHub rate-limit handler sleeps up to an hour and then fails anyway.** `backends/github.rs:127-139`: a 403 sleeps until `x-ratelimit-reset`, then returns the *same 403 response* — no retry — so `release_at`'s `error_for_status()` (`:153`) errors regardless. The wait buys nothing and costs everything: the data lock is held throughout (S27), and every other Shall command on the host blocks. **Observed:** `GitHub Rate Limit reached. Pausing for 2618s...` inside a `--dry-run`. Two further faults in the same six lines: a preview reaches the network at all, and D12's "zero HTTP requests when pinned and locked" does not save a machine whose `locks/github.toml` is empty — which is every machine straight after `adopt`. **RULED (owner, 2026-07-23): capped, retried once, and the cap is settable.** `rate_limit_max_wait_secs` in `preferences.toml`, **default 30**, then one retry, then an error naming the host and the reset time. Settable because a CI job and a laptop want different answers; short by default because sleeping an hour on a held lock makes a command look hung, and a command that looks hung is the one people kill — which is precisely what arms S24. → **Phase 5** |
| **S27** | **FIXED 2026-07-23.** The mismatch is gone with S26 (the longest legitimate wait is now 30s against a 120s patience, and the 120s carries that as its stated reason). The message was the other half and it was **wrong in a way no timing change fixes**: it advised removing `shall.lock` "if nothing is running", but the lock is an OS lock on an open handle, released when the holder exits — so a lock still contended after the wait *proves* a live holder, and the advice was to take the lock away from a running writer. It now says that, names the holder and the wait, and offers waiting or stopping that run. Original entry follows.<br><br>**The lock's patience is shorter than the sleeps it must outlast.** `main.rs:356` waits 120s for the data directory; `github.rs:137` parks a run holding that lock for up to 3600s. Every command issued during a rate-limit pause fails with "the data directory is locked … remove `shall.lock` if nothing is running" — advice that invites the user to delete the lock out from under a live writer, which is the race the lock exists to prevent. *Recommendation:* fix S26 and the mismatch mostly goes; the message must never suggest deleting the lock while it names a holder it has not proven is dead. → **Phase 5** |
| **S28** | **FIXED 2026-07-23. Three schedulers reported a removal they had not made.** `remove_task` swallowed every failure — `let _ = fs::remove_file(...)` on the systemd timer and service units and the launchd plist, and a discarded exit status from `schtasks /Delete` — so a schedule that could not be torn down was reported as removed and kept firing. Named as its own entry by S24's reading rather than fixed under cover of it. Each provisioner now deletes through one `remove_generated` (a missing file is the wanted end state; any other error names the path) and then **asserts the end state** with the `is_task_active` probe the trait already has — the exit code cannot distinguish "already gone" from "refused" on any of the three, but the probe can. Three unit tests on `remove_generated`. **The sibling this exposed one layer up:** `reconcile_extras` recorded `declared` after a failed undo, so the key left the ledger and the next sync never retried — one warning, then a service Shall no longer knew it owned. A failed teardown now stays recorded; tests `a_failed_undo_stays_in_the_extras_ledger` and its success-path twin. → **Phase 3** |
| **S31** | **FIXED 2026-07-24. `shall install mise:X` reported success and installed nothing** — the fail-silent class, in a shipped backend. `MiseQueryable::info` asked `mise plugins ls --all`, which lists every plugin mise has ever *heard of*, so it answered `Some` for anything in the catalogue; the planner read that as "already installed", skipped the install, and printed **already up to date**. Every `mise:` line in every config was inert. `info` now consults the installed set and nothing else — the catalogue answers *could this be installed?*, which is a different question and the one nobody was asking. **A sibling in the same file, found while fixing it:** `list_installed` ignored mise's own `"installed": false` flag, so a tool stayed listed after `mise uninstall` (mise keeps the entry because the declaration in its config survives) — Shall reporting a package present when it was not, which is drift detection reading backwards. **Found by the `tools` container image on its first ever run**, which is exactly what IV.1 said that image was for; no unit test could have caught it, because none of them runs a real mise. Four tests now pin it against **real captured `mise list --json` output** carrying both states. → **Phase 5** |
| **S32** | **FIXED 2026-07-24, in the harness rather than the product.** The same `tools` run failed `mise: jq is still on PATH after removal` — and that check could never have passed: `jq` is the image's **apt** canary as well as mise's, so `command -v jq` answers about apt's copy whatever mise did, and a mise tool only reaches PATH through `mise activate`, which the image does not set up. Two independent reasons for a vacuous check, which is IV.1's own rule ("grep for something only the right answer contains") broken twice in one line. mise's canary now declares no binary, so no PATH assertion is made; the backend-scoped `list --backend mise` check is the real presence assertion — and it is the one that caught S31. → **Phase 5** |
| **S29** | **FIXED 2026-07-23. Shims escaped the sandbox, and only a fixture's false `dry_run` was hiding it.** `ShimManager::new()` resolved `~/.local/bin` from `dirs::home_dir()` at three call sites, so `Config::sandboxed` could not move it and **`cargo test` deployed an executable named `rg` onto the developer's real PATH** — observed, not theorised: the file was there, 35 MB, a copy of the test binary. It could not happen while `TestKernel` set `config.dry_run = true`, which is the second time in one session that fixture has been the reason a defect was invisible (see S25). `bin_dir` is now a `Config` field — `#[serde(skip)]`, because a repo must not be able to move Shall's shims onto a machine's PATH by declaration — and `ShimManager::new` is **deleted**, not left beside it: a constructor that finds the directory itself is the one a sandbox cannot move. `sandboxed_puts_every_path_under_the_sandbox` now asserts seven paths. → **Phase 5** |
| **T5** | **FIXED 2026-07-23. The plaintext is restricted before it exists, on all three platforms.** `CommandExecutor::write_secret` restricts the *temporary* file and then renames it into place, so the destination path never holds a readable secret — that is what "created restricted" can mean when the OS has no way to create a file with an ACL. Unix asserts `0600` rather than assuming `tempfile` set it; **Windows gets the real answer rather than the documented excuse**: inherited ACEs stripped and only the running user granted, via `icacls`, and a failure is an error because a secret nothing protects must not be written at all. The test runs `icacls` and checks both halves — it passed on the owner's Windows box, which is the platform the ruling was written about. → **Part XII** |
| **T6** | **FIXED 2026-07-23 (the ruled half).** Removing a `link:` declaration restores the file that was there before it and deletes the backup, which answers sub-question 3 and dissolves 2 — a backup that is put back cannot accumulate. **The fix found a worse defect in the same three lines:** the teardown was handed the declaration's **source**, so undoing a `link:` deleted the file in the user's own dotfiles repo and left the deployed copy in place. The ledger keys a link by its **destination** now (`extra_key`), which also means editing `@target=` undoes the old destination instead of orphaning it forever. **Sub-questions 1 and 4 closed 2026-07-26:** the opt-out is the per-line `@backup=no` (no machine-wide key — a setting that turned backups off everywhere would travel between machines and the file it silently stopped preserving would be one somebody hand-wrote), and there is **no** orphan-cleanup command, because restore-on-teardown means backups cannot pile up. Built in `link.rs::wants_backup`. → **Part XII** |
| **S30** | **FIXED 2026-07-23, found while adding `rate_limit_max_wait_secs` to every layer that carries it.** `examples/preferences.toml` documented `[retention.generations]` and `[retention.manifests]` — both deleted with the generation format a phase earlier, both silently ignored because `RetentionConfig` did not deny unknown fields. Nothing checked the example at all: the template has a parse test and its long-form twin, the file a reader is likelier to paste from, had none. `RetentionConfig` now denies unknown fields (the reasoning already written on `Config`: a table nobody reads is a setting that documents itself as working), the dead blocks are gone, and `the_shipped_example_parses_too` asserts the example parses **and** that its `rate_limit_max_wait_secs` matches the built-in default. → **Phase 5** |
| **T2** | **FIXED 2026-07-23.** `refuse_target_in_repo` runs at the top of the decrypt branch, **before the tool is launched** — a refusal that happened after decryption would have the plaintext in memory and the reason to exist already gone. It canonicalises both paths where it can (a target that does not exist yet cannot be, so the written paths are compared then), and the error names the destination *and* the repo root, because "somewhere inside your repo" is not something a reader can act on. Original entry follows.<br><br>**Nothing stops a decrypted secret being written back into the git-tracked config repo.** `resolve_target` (`link.rs:19`) accepts any path; a `link:./secrets/token.age@target=./secrets/token@decrypt=age` writes the plaintext beside the ciphertext, inside the repo, and the next sync commits it. A secret in git history is a rotated secret, so the failure is unrecoverable rather than merely bad. *Recommendation:* refuse at resolve time when `@decrypt` is set and `@target` resolves inside the config root. **The related question the fix should answer at the same time:** `bundle` copies the config root (X.5), and its promise that a backup is safe to hand to someone depends on exactly this invariant holding. See **XII.5**. → **Part XII** |

| **S33** | **The suite is green only on a host that already has a git identity, and CI has been red since 2026-07-23 because of it.** `tests/feature_logic_tests.rs:90` calls `git.commit_all("baseline").unwrap()` against a fresh temp repo. On a machine with no global `user.name`/`user.email`, git refuses; Shall reports it exactly as it should — the error names the two commands to run — and the test panics on the `unwrap`. **The product is not at fault here, the fixture is:** `GitManager` fails loud and names the fix, which is the behaviour V-P3 asks for. It passes on the owner's Windows box because that box has an identity, so `cargo test --all-targets` was *honestly* reported green on this host and failed on both `ubuntu-latest` and `windows-latest` in the same tree — a green that is a property of the developer's git config, not of the code. **The sibling is what makes this an entry rather than a chore:** `src/core/git.rs` already carries an `identify()` helper written for this exact reason — its own comment says *"a test repo on a machine with no global `user.email` would otherwise fail in `git commit` rather than in what it is testing"* — and the one test outside that file that commits never got it. One fixture learned the lesson; its twin, one directory over, did not. **Family checked:** `commit_all(` / `git.init()` outside `src/core/git.rs` finds this test and nothing else under `tests/`; `src/app/bundle.rs:135` reads a repo it does not create, so it cannot hit this. **FIXED 2026-07-26.** Not by copying `identify()` — by removing the need to remember it. `TestKernel::new` and a new `git_or_skip()` gate in `git.rs` set `GIT_AUTHOR_*`/`GIT_COMMITTER_*` once per test binary and point `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM` at absent paths, so the tests read *no* host git config: not the missing identity that caused this, and not a present `commit.gpgsign` that would have caused the next one. `identify()` and its three call sites are deleted (NO LEGACY). The mechanism matters more than the fix: it is now impossible for a new test to forget the setup without also forgetting the skip guard, which fails loudly. **The one thing this cannot prove locally is the negative** — that CI is green — since this host has an identity either way; that is what the next CI run is for. |
| **S34** | **FIXED 2026-07-26. The same shape as S33, on the other two platforms, and it was hiding behind it.** With S33 red on Windows, nobody looked at the Linux and macOS columns — both were also red, on a *different* test. `app::eval::tests::sources_are_repo_relative_and_forward_slashed` asserted `repo_relative(r"C:\repo\modules\x.txt:7", Path::new(r"C:\repo")) == "modules/x.txt:7"`. A backslash is a path separator on Windows and an ordinary filename character everywhere else, so `strip_prefix` splits that into components on Windows and treats the whole string as one name on unix — where the assertion returns `C:/repo/modules/x.txt:7` and fails. **The product is right and the test was wrong**: Shall never sees a `C:\` origin on Linux. Split in two — `sources_are_repo_relative` on platform-native paths everywhere, `windows_sources_come_back_with_forward_slashes` under `#[cfg(windows)]`. **Family checked:** 15 other sites hand a `r"C:\…"` literal to something, and only this one asked `Path` for *path semantics* — the rest (argv quoting, grammar parsing, the `setting` regex, `windows_shim_wrap`'s `.extension()`) treat it as an opaque string and are platform-independent; `config/config.rs:850,864` and `backends/onboarder.rs:1110` already pick their root with `cfg!(windows)`, which is the pattern this now follows. **Verified on Linux, not argued:** the suite was run in the `shall-it-ubuntu` container, which also has no global git identity, so S33 and S34 are both proven fixed on a host that reproduces both. |
| **S35** | **FIXED 2026-07-26. `macports` ran a program that exists on no Mac, and no test on any platform could have seen it.** `register_macports` left `binary: None`, which means *the backend name is the command* — so Shall probed `command -v macports` and would have run `macports install jq`. MacPorts is the collection; **the program it ships is `port`**, which the row's own comments already said (`port installed requested`). The consequence was worse than a failed command: `is_available()` answered *false* on a Mac with MacPorts installed, so the backend never came up READY, `doctor` reported it absent, and a `macports:` line was refused as an unusable backend — a whole backend dead on arrival, quietly. **Found on Windows**, by the test written for finding 3. **Family checked:** no other generic row has a program different from its name — `binary: Some(…)` appears nowhere else at the top level, and the near misses are all correct (`pkg_add` really is `pkg_add`, with `remove_binary: pkg_delete`; apt's `list_binary`/`search_binary`/`enumerate_binary` split is deliberate). The guard against a repeat is `every_os_native_backend_sends_the_argv_its_manager_expects`, which asserts the **program name as well as the verbs** for 26 generic backends, on every host — the leading word being the part nothing else checked. |

| **S36** | **FIXED 2026-07-27. This is S32 again, and it came back because S32 was fixed at the instance.** The `tools` image reported `go: hello is still on PATH after removal` — on a removal that had worked, as the line directly above it said (`go: hello is gone from list`). `assert_binary_gone` asked `command -v <binary>`, which answers *"does this name resolve anywhere"*, not *"did this backend's file go away"*. cabal's canary is `hello`, cabal's remove-mode is `unsupported` (it has no uninstall verb), so **cabal's `~/.cabal/bin/hello` is installed early and stays for the rest of the run by design** — and go's canary binary is also `hello`. go removed `/root/go/bin/hello` correctly and was handed cabal's deliberate leftover as its own failure. **S32 was this same sentence with `jq`, apt and mise in it**, and the fix then was to give mise a canary with no binary — one canary corrected, the predicate that produced the wrong answer left standing, so the next name collision found it again. **Fixed at the predicate this time:** the check now compares against what the name resolved to *before the install* — whatever the install added must be gone, and whatever was already there is not this backend's to answer for — so it is immune to name collisions rather than merely clear of the two we know about. Distinguishing verdicts are kept: a binary that reappears in this backend's own directory still fails, and `removal_leaves_binary`'s documented quirks still soften only when the leftover actually happens. **Family checked:** both harnesses carry the identical function and both are fixed (`docker/integration/run-in-container.sh`, `scripts/integration-windows.sh`); the remaining bare `on_path` assertions are the primary package's, where the harness installed the only copy by construction, so they are strict on purpose. `scripts/harness-logic-test.sh` pins all six verdicts against the lifted function bodies and runs on **every push**, because both S32 and S36 were found by a nightly job a day late. |
| **S37** | **FIXED 2026-07-27. The macOS sweep ran nothing at all, and reported passes.** `macos-native` had never executed; its first run exited 1 with every check reading `rc=127`. The cause is one line: `TO="timeout 900"`, and **`timeout` is GNU coreutils — macOS ships neither it nor `gtimeout`**. Every wrapped call was `command not found`. **What makes this an entry rather than a typo is the second half:** `nok` counted *any* non-zero exit as "correctly refused", so a run in which no command executed still printed `PASS … (correctly refused)` for its negative paths — the harness's own words, S32's rule, broken again: an assertion that cannot fail. **Three fixes, at three levels.** The wrapper falls back `timeout` → `gtimeout` → unbounded-and-says-so; the preflight now runs `$TO "$SHALL" --version` instead of `command -v "$SHALL"`, so a broken *wrapper* is caught and not just a missing binary (the old check passed while every invocation 127'd); and `never_ran()` makes 127/126/124 a hard failure in `nok` in **both** harnesses, so a command that never reached its own decision can never again be scored as a refusal. CI installs coreutils on the macOS runner so the bound stays real there. **A coverage gap the same run exposed:** the audit named `add`, `eval` and `try` as only ever `--help`'d — the host harness, which claims to mirror the container one section for section, never invoked them. All three now run for real; `try` is pointed at an image that cannot exist, so it refuses on every host without spending ten minutes building. |

| **S38** | **FIXED 2026-07-27. The host harness was driving a command surface that no longer exists, and the result was not a failure — it was silence.** `doctor`, `status`, `absent`, `unmanaged`, `conflicts` and `audit` were folded into `check <section>`; `run-in-container.sh` was updated and `scripts/integration-windows.sh` never was, so clap answered *unrecognized subcommand* (exit 2) six times. **The damage is not the six checks.** One of those calls is `READY_LIST=$(lx doctor …| grep '^\[READY\]')` and another is `ALL_BACKENDS=$(lx doctor --json …)` — both came back empty, so section 12 (every manager's real lifecycle) and section 13 (plan-smoke over the whole registry) each iterated over an empty list and reported **nothing wrong at all**. The macOS sweep printed `READY backends:` and `registered backends: 0` and carried on. A stale name does not announce itself as reduced coverage; it announces itself as none. Mapped to the real surface (`check health`, `check drift`, `check`, `check absent`, `check unmanaged`, `check conflicts`, `check security`) and the `--json` registry extraction re-pointed at `check health --json`, whose `"backend"` keys are unchanged. **A second fault in the same file, from the same drift:** only the aggregate `check` exits 2 under U21's table (findings, not failure) and the host harness asserted it with a hard `ok` — it had never had the `answers()` helper its twin has. Added, and used for the two aggregate-`check` assertions. **The guard is the point:** `scripts/harness-logic-test.sh` now takes `SHALL_BIN` and fails when a harness invokes a subcommand the binary does not have. Against the previous revision it names all six. |
| **S41** | **FIXED 2026-07-27. `restore --force` could not overwrite a read-only file — and a restored config root is full of them by construction. The first product bug of this sweep.** `bundle` copies *the whole config root*, that root is a git repo (II.13), and **git writes its loose objects and packs at 0444**. `fs::copy` carries permission bits across, so the first restore lands a tree of read-only files and the second — which is the only thing `--force` is *for*, restoring onto a machine that already has a config — met a destination it could not open for writing. It failed with `Error: I/O error: Permission denied (os error 13)` **naming none of the several hundred paths it had copied**, which is its own defect against V-P3 and cost a full CI round to place. **Why nothing caught it:** the container harness runs as **root**, and root bypasses the permission check entirely, so every Linux run was green; it reproduces on Windows too (`Access is denied`, os error 5), so this was never macOS-specific — only non-root. **Fixed at one function, not four call sites.** `copy_over` removes an existing *file* first (a directory in the way is a different fault, and deleting one to make room would turn a mistake into data loss) and names both paths on failure. It lives in `utils/file.rs`, whose deploy helper already carried this exact lesson in a comment — *"the old entry must be gone before the new one is made"* — which `bundle.rs` had never learned. **Family fixed in the same change:** all four copies in `bundle.rs`, plus `github.rs`'s artifact install and `web.rs`'s download (both write over a previous copy on reinstall, and release archives routinely ship 0444). **Checked and NOT changed:** `link.rs`'s deploy path already goes through the helper that removes first (only the Windows cross-drive fallback copies directly, into a target the same function has just checked); `utils/archive.rs` and `verbs/setup.rs` write to paths their callers have just created. Two tests: the read-only overwrite, and that a failed copy names its file. |
| **S40** | **FIXED 2026-07-27. A section of the "Windows/macOS" harness was Windows-only, and on macOS it tested nothing while reporting two failures.** Section 11b stages a manager that *cannot answer* (V.7c: silence is not a no) by shadowing cargo's `search` with a shim on PATH. The shim was written as `cargo.bat` — correct on Windows, where a bare `cargo` resolves through PATHEXT, and inert on macOS, where resolution is by executable bit. So nothing was shadowed, cargo answered normally, and the two assertions that depend on a silent manager failed for the one reason their names cannot express: there was no silent manager. Both branches are now written — `.bat` under MINGW/MSYS/CYGWIN, an executable `#!/bin/sh` shim everywhere else, the same one `run-in-container.sh` has always used. **The class is the file's own premise:** its header says it mirrors the container harness *section for section*, and three separate faults this week (S38's stale verbs, the four `ok`-vs-`answers` sites, and this) were all places where it had stopped doing so. The `SHALL_BIN` guard catches the first class; this one is caught only by running it on a Mac, which is now nightly. |
| **S39** | **FIXED 2026-07-27 — and it was the harness's own litter, not the product. `sed -i` is not portable, and `|| true` hid that it had done nothing.** Section 6 installs `linux-no-such-pkg-zzz` on purpose to prove an unresolvable name fails loudly, then deletes the line it leaves behind, because — in the words of the comment sitting directly above it — *"left in, it is committed and then reinstalled by the `rollback` in section 9, which fails there instead of here."* That is precisely what happened. The deletion was `sed -i '/…/d' file 2>/dev/null || true`; **BSD sed reads the argument after `-i` as a backup suffix**, so on macOS it took the script as the suffix, failed, and was silenced twice over — by the redirect and by the `|| true`. The name stayed in the manifest, and every later converge tried to install it: `rollback HEAD`, `activate` and `restore --force` all exited 1 with `brew failed (exit 1): No available formula with the name "shall-no-such-pkg-zzz"`. **Shall was right every time** — it refused to converge a package no manager can resolve, and said so. **Two wrong readings on the way here, both from the harness hiding output:** first that these were panics (there is no `panicked at` anywhere in the run), then that they were product bugs in a converge path. Replaced with the same read-filter-move `undeclare_canary` already used, in **both** harnesses — the container's copy works only because every image ships GNU sed — and the removal is now *asserted* (`nok … grep -q`) instead of hoped for. The `grep -v` result is moved unconditionally rather than with `&&`, because a file whose only line matched leaves grep exiting 1. |
| ~~**S39 — OPEN**~~ (superseded by the entry above; kept for the reading it corrects) | **Three commands panic on macOS.** With S37 fixed, the macOS sweep executed for the first time and `rollback HEAD`, `activate <profile>` and `restore --force` each exited 1 with a Rust backtrace (`__mh_execute_header` frames, which is what an unsymbolised macOS backtrace looks like). **All three converge**, and converging is a no-op on the Windows box where they pass — that machine already matches, so the failing path is never entered. Not reproduced on Windows; not yet diagnosed, because **the harness hid the message**: failures printed `tail -6`, `RUST_BACKTRACE=1` is set in CI, and the last lines of a failure are stack frames — on macOS a column of identical `__mh_execute_header`, the release binary carrying no symbols. **Corrected 2026-07-27: these are not panics.** The second macOS run found no `panicked at` anywhere, so they are ordinary `Err` returns whose backtrace is printed because `RUST_BACKTRACE` is on — which the first version of the fix, keyed on the word `panicked`, still could not show. `excerpt()` now drops backtrace frames entirely and prints what is left, in both harnesses, so the next run reports the error. **Do not close this on the strength of the harness fix — three commands still exit 1 on macOS and nobody has read why.** |

| **S42** | **FIXED 2026-07-27. Under a real terminal Shall discarded the output of every command it ran, and what you saw instead looked like the answer.** `RawExecutor::execute` asked whether *Shall's own stdin* was a terminal and, when it was, handed the child all three handles. Inherited stdout means `output.stdout` comes back **empty**, and that is what all 79 `run_output` call sites parse — how Shall learns what is installed, at what version, what a search returned, what a repo list contains. Measured on one machine, one command: `shall list -b apt` parsed **609 rows piped and 1 under a pty**. It is deceptive rather than obviously broken, because `dpkg-query`'s raw output bleeding through the inherited handle reads like a package list. **Fixed by making capture a property of the call:** `RawExecutor` carries a `ChildStdin` policy, the reader layer closes stdin and the mutating layer may share it, and **both capture stdout and stderr always** — so `sudo` can still ask for a password (it writes its prompt to `/dev/tty`) while every parser is fed. A long mutation still shows progress: the bytes are mirrored to **stderr** as they arrive when a terminal is attached, never to stdout, which carries Shall's own answer. **Why nothing caught it:** not one of 1,324 tests, four container lifecycles or three OS builds ever ran the binary attached to a pty — every gate in the repo pipes every handle, so CI *could not* observe this and its passing was never evidence against it. `tests/pty_tests.rs` closes that gap with `script -qec` and a stub manager on `PATH`, and is a named step in CI's fast half; it was watched failing against the old behaviour first. Ruled as U40; the rule is II.12c and the reason is V.84. |
| **S43** | **FIXED 2026-07-27, with S42 and from the same root. A read-only command hung waiting for a keypress.** Because stdout was inherited, children concluded a human was watching and turned on interactive behaviour: `shall status` invokes `systemctl`, `systemctl` piped itself into a pager, and the run stopped at `lines 1-16/16 (END)` until it was killed. The measurable half is that output became **non-deterministic** — three identical `shall status` runs under a pty printed **80, 640 and 83 lines**; piped, 631/630/630. **S42 removes the trigger but is not the fix**, because `$PAGER`/`$SYSTEMD_PAGER` in the environment forces a pager regardless. Suppressed in the one env map every spawn inherits (`SYSTEMD_PAGER`, `SYSTEMD_LESS`, `PAGER`, `GIT_PAGER`), which is the single site rather than a list of call sites to keep in step. **Family:** every systemd row in `init_providers.toml` now carries `--no-pager` — all five actions plus `list` and `status`, not only the two that print a screenful, because systemctl decides the same way for all of them — and so does every `systemctl` call in `app/scheduler/mod.rs`. **Checked and NOT changed:** `core/git.rs` spawns git with `std::process::Command::output()`, which already pipes both streams, so git never pages there; `app/insight.rs`'s git call goes through the executor and is covered by `GIT_PAGER`. No `journalctl` or `dnf` call site exists in the tree (grep). |

| **S44** | **FIXED 2026-07-27. apk and gem reported a repository removed that was still there, and apt and apk could never have removed one at all.** Two faults in one row-driven surface. **(a) `{url}` was never substituted on the removal path.** `RepoManager::remove_repo(name, sudo)` has no URL, but two rows need one — apk's `sed -i '\|{url}|d' /etc/apk/repositories` and gem's `sources -r {url}`. The gem case passes the literal `{url}` as an argument; the apk case is worse, because **sed searches for the literal text, matches nothing, and exits 0** — `run()` enforces exit status, sees success, and Shall says the repository is gone. Fixed by resolving the URL before the argv is built (from the manager's own `list_repos`, or from the identifier when the manager's listing *is* a list of URLs), refusing when it cannot be identified, and running `reject_shell_meta` on what comes back because it lands in an `sh -c` string. **The class, not the instance: an unsubstituted placeholder is now a hard error** — after substitution, any argument still matching `{[a-z_]+}` refuses before anything runs, so the next row with a new placeholder fails loudly instead of silently. **(b) The repository rows named a program in the argument position.** apt's row began `add-apt-repository` and apk's began `sh`/`cat`, but `add_repo`/`remove_repo`/`list_repos` all ran `core.binary()` — so the real commands were `apt add-apt-repository -y <url>` and `apk sh -c …`, which both managers refuse. **`repo add`, `repo remove` and `repo list` had never worked on apt or apk.** `ManagerConfig` gains `repo_binary` and `repo_list_binary`, the same shape `list_binary`/`search_binary`/`enumerate_binary` already had for exactly this reason, exposed in `adapters/backends.toml` and documented. **Family checked:** every other row's leading word really is its manager's own verb (`zypper addrepo`, `winget source add`, `choco source add`, `scoop bucket add`, `gem sources -a`) and a new registry test pins the program for all seven; `dnf.rs` and `pacman.rs` implement `remove_repo` by hand with `format!`, carry no template, and are unaffected. `add_repo` always substituted both keys and still does — asserted, so the new guard cannot break the path that worked. |

| **S45** | **FIXED 2026-07-27. A failed upgrade uninstalled the package, and the rollback that did it reached no guard.** Three faults that compound into one. **(a) An upgrade was compensated by an uninstall.** `rollback` turned every `GraphAction::Install` into `remove()`, which is right only when the package was absent before — and `spec_is_missing` schedules an `Install` node for a **version or channel change on an already-installed package**. So a failure anywhere in the graph uninstalled software the user had, instead of reverting it. The transaction now records a `Prior` per node *before the node runs*, and compensates an upgrade by reinstalling the previous version. **(b) `needs_change` failed open.** `match q.info(…) { Ok(Some(p)) => p, _ => return Ok(true) }` read *"I could not ask"* as *"not installed"* — and under S42 that was **every package**, so every managed package got an Install node, each succeeded trivially against an already-present package, each landed in the history, and one later failure rolled back across the whole set. **A mass-uninstall reachable from an ordinary interactive `sync`, out of two independently reasonable defaults.** An `info()` that errored is now an error, the way `search_output` already separates "no result" from "could not answer" (V.7c). **(c) The guard did not cover it.** `transaction.rs` had **zero** references to the guard: `guard::enforce` runs at plan time over the planner's `Remove` nodes, and rollback's removals are issued at execution time, so `protected_packages` and OS-essential protection never applied to them — while II.10 said every removal path calls it. Rollback now calls `guard::protection_of` before any compensating removal; a refusal stops that one removal, the package stays, and the rollback reports itself incomplete and names it (**ruled U41** — the partly-applied state is reported, never hidden). **Also fixed in the same change:** a prior state the manager could not report is `Prior::Unknown` and is never read as absence (the package stays, and is named); and the `Remove` arm's reinstall carried `options: HashMap::new()`, so a rolled-back removal came back at whatever was newest — it now comes back at the version it left at. **Verified by watching them fail:** the three behavioural tests fail against the previous compensation logic, and the guard test fails with the guard call removed. Rule in II.10, reason in V.85. |

| **S46** | **FIXED 2026-07-27. Two panics on the path whose premise is that nothing runs.** `DryRunOutput::faulted` and `From<DryRunOutput> for Output` needed an `ExitStatus`, which has no public constructor — so they **spawned `/bin/false` or `cmd /C exit 1` and `.expect()`d the result**. A host stripped of `/bin/false`, or any condition that makes a spawn fail, panics inside a dry run. Both platforms expose the raw form (`ExitStatusExt::from_raw`; on Unix the exit code sits in the high byte of a wait status), so no process is spawned at all. These were 2 of the 9 `unwrap`/`expect` sites the review flagged in `core/executor.rs`. **Not a bug, corrected from the review:** the review reported the runtime banner in `app/context.rs` still hardcoding `v6.0.0` as a second source of truth. A grep of `src/` finds no hardcoded version anywhere — `lib.rs` derives `VERSION` from `CARGO_PKG_VERSION` and `cli/args.rs` reads the same. The `v6.0.0` line the review observed came from the stale July binary on disk, not from this tree. **What remains is `Cargo.toml`'s `version = "0.1.0"` against a CHANGELOG that records a released `[6.0.0]` and current work as `[Unreleased] — v7`, and that number is the owner's to pick.** |
| **S47** | **FIXED 2026-07-27. A field called `attempt` that held a retry count.** `TaskResult.attempt` was set to `attempt - 1` and passed to `record_operation`'s `retry_count` parameter — so the arithmetic was right and the *name* was wrong, which is why the production-readiness review read it as an off-by-one reporting "0 attempts for a first-try success". It reports 0 *retries*, which is correct. Renamed to `retries`, with a doc comment saying what it holds, so the next reader does not correct a bug that is not there. Invisible from outside the program: one internal field and its one consumer. |

| **S48** | **FIXED 2026-08-09. Three gates written to prove they could fail asserted that a string literal contained a substring it visibly contained, and six more tests asserted nothing at all.** Found by the 2026-08-09 whole-repo review as finding 4; fixed first, because the other twelve findings are verified with these. **The three oracles.** `output_is_sanitized_tests.rs:171`, `ledger_file_rules_tests.rs:172` and `grader_refusal_exit_code_tests.rs:399` each carried the right doc comment — one of them quotes the standard *"do not test your own oracle by assuming it works"* — and then declared a `&str` two lines above the assertion and asserted on that. **None of them called its scan.** Replacing the scan's predicate with `false` left all three green, which was verified by doing it: the mutation reddened only the rewritten oracles, and the real scans stayed green throughout, which is the whole reason an oracle has to exist separately. Each predicate is now a named function at module scope — `is_raw_stdout_read`, `offences_in`, `says_it_is_refusing`/`enclosing_builder`/`call_sites`, three of them lifted out of the loop or the test body they were nested inside — driven over a planted offender and every shape that must not read as one: a sanitized read, a stderr read, a read past `#[cfg(test)]`, an `ALLOWED` exemption, a comment, a definition, `Error::Validation` where `Error::Refused` was wanted, and a wrap three lines *below* the call. Each was watched failing under a mutated predicate before the mutation was reverted. **The six vacuous ones.** `security_and_resiliency_tests.rs:399` ran five malformed specs through the resolver and `.ok()`d every result — zero assertions, so it passed whether they were refused, accepted, or the resolver was deleted; it now asserts each is refused *and* that the refusal names its origin, with a well-formed spec as the control. `shell_lifecycle_tests.rs:99` wrote a manifest, read it back, and split it with its own copy of `auto_shell`'s five lines — a test of `str::lines`; the reader is now `shell::manifest_lines`, which the test drives and which routes through the grammar's `strip_comment` instead of being the **fourth** hand-rolled comment rule (so `brew:jq  # note` works and a URL fragment survives). `integration_test.rs:101` recorded metrics and asserted only that `print_summary` did not panic; `MetricsCollector` gains `operations()` and `totals()` and every recorded field is now read back, including `batch_size`, which had no other reader. `dag_test.rs:140` claimed per-backend lock isolation from two `run_exclusive` calls returning `Ok` — which one global mutex also satisfies, and mock commands were instantaneous so there was nothing to contend over; `MockExecutor::set_delay` makes a command take time, and the test now asserts two backends overlap *and* that two calls to one backend do not. Watched failing against a mutated `run_exclusive` keyed on a single global lock. **Family:** eleven binaries already carried a genuine oracle and were left alone; `phase_is_the_sync_order_tests.rs:183` is the model all three rewrites follow. Rule in II.23, reason in V.153. |

| **S49** | **FIXED 2026-08-09. The firewall's guard scope round-tripped through a string, and the two vocabularies did not overlap — so an unattended `watch` tick closed ports under the name `sync`.** `verbs/sync.rs:41`'s `scope_label` turned a `GuardScope` into `"an unattended watch tick"` or `"sync"`; `apply/firewall.rs:275`'s `guard_scope` turned a string back into a `GuardScope` by matching `"purge-undeclared"` / `"watch"` / `_`. **Both named arms were unreachable.** Every firewall teardown resolved to `GuardScope::Sync`, and `N7` is what makes that expensive rather than cosmetic: an unattended tick **reverts by default**, so the one path the file's own header calls *"the dangerous one, because nobody is there to read a refusal"* was the path whose refusals could not say so. `GuardScope` is `Copy`; it is passed. `scope_label` and `guard_scope` are both deleted, `Firewall::apply` and `lockout_refusal` take the enum, and the enum gained `during()` — the prose half of its own vocabulary, written out per variant, because a catch-all is exactly how the deleted label came to answer `"sync"` for nine of twelve scopes. **Why nothing caught it:** `tests/a_firewall_teardown_is_a_removal_tests.rs` was written for this promise and asserted `GuardScope::Sync.as_str() == "sync"` — a getter returning what a constructor took — under a comment saying *"the mapping is private, so this asserts the property through the public enum instead"*. The private mapping it declined to test is the broken one. The compile error that surfaced when `lockout_refusal` took the enum landed in `model/firewall.rs`'s own test, which had been **handing it the prose string directly** — the test and the producer agreed, and neither had ever met the consumer. Now asserted as a round trip, with two scopes required to produce different refusals. **Family swept:** `scope_label` and `guard_scope` were the only two scope-as-string hops in the tree (grep); `enforce_extras`, `enforce`, `enforce_deliberate` and `enforce_installs` all already took the enum. Rule in II.10, reason in V.154. |

| **S50** | **FIXED 2026-08-09. `shall history` could run a full rollback and sync — the entire install/remove path, `state.save()` and all — with no data lock held.** A hand-maintained `&[&str]` of twenty-one names in `main.rs`, seventy lines from the `Commands` enum, decided whether a run took the 120-second exclusive lock on `data/`. `"history"` was on it. `handle_history` opens a TUI; `HistoryAction::Rollback` reaches `handle_rollback` → `handle_sync`. **The identical function reached through `Commands::Rollback` was locked** — one function, two doors, two locking regimes, decided by a string list. And the list was wrong in the other direction too: `fleet` drives other machines over SSH, touches no local state, and was absent from the list, so it took the writer lock for a purely remote report. **Its own test docstring recorded that twelve of its thirty-three entries once named commands the program did not have** (`status`, `doctor`, `unmanaged`, `absent`, `insight`, `show`, `audit`, `outdated`, `log`, `locate`, `metrics`, `verify`) — and the two tests guarding it **both guarded invention**: that every name on the list was real. Nothing guarded omission or misclassification, which is the half that costs an entry out of `registry.json`. **Fixed as `Commands::writes()`, an exhaustive match on the enum.** A subcommand added later does not compile until it answers — strictly stronger than the argv read's "unknown means writer", which was a default nobody revisited. `acquire_data_lock` takes the parsed `&Commands` (it was already called *after* clap parsed, so the argv read was never needed), and `READ_ONLY_COMMANDS` is deleted. `run_user_verb` now locks when **any** step writes rather than unconditionally, so a verb of five readers stops holding the writer lock and a verb whose third step syncs takes it before the first step runs. **`history` stays a reader and takes the lock where the rollback fires** — locking the whole command would reintroduce the `edit`-blocks-on-`$EDITOR` problem `AU6` records, where one person reading a manifest in vim stopped every other Shall on the machine. **Both new tests were watched failing** against a tree with `History` moved to the writer arm. Rule in II.24, reason in V.155. |
| **S51** | **FIXED 2026-08-09. Every JSON reader in the repo parsed from byte zero, and composer prints a banner.** `composer global show --format=json` is preceded by `Changed current directory to /root/.composer` on every machine that has ever run `composer global` — which is all of them. `parse_composer_json` did `serde_json::from_str(output).unwrap_or_default()`, so that banner was a syntax error, `Value::Null` answered `None` to every accessor, and the installed listing `sync` plans from was **empty on every machine with a global composer config**: every declared PHP package a fresh install, every removal dropped. **The comment explaining that exact banner was already in this repo, at `registry.rs:1933`, two lines below the wiring — attached to the `outdated` probe, whose parser did `text.find('{')`.** Two lines apart, one fixed. The fix is one shared reader, `parsers::json_document`, which finds the document rather than assuming it starts the output and stops at the end of the first value rather than rejecting whatever follows it. Applied to the whole family, which is every `from_str` on a manager's stdout: `parse_composer_json`, `parse_composer_search`, `parse_composer_outdated` (its bespoke `find('{')` deleted — one implementation, not two), `parse_npm_style_json` (npm/pnpm), `parse_pip_json`, `parse_pipx_json`, `parse_conda_history`, `parse_conda_list`, `parse_conda_search`, and `parse_nix_search`. Watched failing: with `json_document` replaced by a plain `from_str`, six tests go red, including the pre-existing `composer_reads_latest_past_its_own_banner`. |
| **S52** | **FIXED 2026-08-09. `appimage:` re-downloaded every declared AppImage on every sync, for ever.** `install` keys its state by `spec.name`, which for this backend *is* the URL — and that is also what the declaration says. `fetch_installed` reported `url.split('/').next_back()`, the basename. So `info(url)` never matched its own state file: every declared AppImage read as absent, `sync` re-fetched all of them every run, and a removal could never find the row it was meant to delete. **`btrfs.rs` carries a test for this exact family, dated 2026-07-30, whose comment reads *"A name `list` does not return is a package `sync` believes is absent: it re-creates it on every run, for ever."*** Family diagnosed, named, tested — and fixed in one member. `web.rs`, this backend's twin, never had it; `github.rs` reports its state keys verbatim and never had it either. One line, plus the test `btrfs` already had. |
| **S53** | **FIXED 2026-08-09. Three backends answered `info` from a catalogue, so `install` reported success having installed nothing.** `Queryable::info` means *is this installed here, and at what version* — the planner's `spec_is_missing` and the transaction's `prior_state` both read it that way, and `shall info` prints "is not installed on this machine" from it. The trait method carried **no doc comment at all**, and three implementations answered *could this be installed?* instead: `vscode` POSTed to `marketplace.visualstudio.com` and returned `Some` for anything published, at the marketplace's *latest* version; `brew info --json=v1` prints a full record for any tapped formula, and `"installed": []` — the one field that distinguishes the two — was read for its properties while its emptiness was ignored, with the version taken from `versions.stable`; `snap info` answers from the store, and the local helper `installed_state` **already returned `None` for a store-only report**, with a test saying so — `info` was the caller that did not ask it. Each produced the same three failures: `shall install <b>:x` reported *already up to date* while installing nothing, a `@version=` pin compared against the newest published version and so re-installed for ever once upstream moved, and `shall info` claimed a package was on a machine that had never seen it. `vscode` additionally made one rate-limited HTTPS POST per declared extension on every plan. **`mise.rs:183` carries this bug's obituary in a doc comment, found by the `tools` container on 2026-07-24** — the test that would have caught the other three already existed, written against the fourth. Fixed in all three, with the contract written onto the trait method so the next backend is told. The other eleven `info` implementations were read and answer from `list_installed`/`scan`/state; `brew`'s keg prefix, `snap`'s `Q20` confinement and `D13` channel all still reach the planner. |

| **S54** | **FIXED 2026-08-09. `or_unrecognised`'s JSON arm returned `Ok(empty)` for any output holding a parseable document, which disabled `LX-1` for the five backends that used it.** The arm was added for a real bug — `pipx list --json` on an empty machine prints four lines of JSON that are neither prose nor package rows, so the line count called a correct answer a format change, measured on a real Windows box 2026-08-07 — and it was written as *if the output parses as JSON, return `Ok(found)`*, **without asking whether `found` was empty**. So npm renaming `dependencies`, or pip capitalising `name`, was a silently empty machine: install everything, own nothing, drop every removal. npm, pnpm, pip, pipx and composer all reached that arm. **Six sites elsewhere already hand-rolled the correct rule** — conda×2, dotnet, pixi, winget, scoop, plus the custom-backend onboarder — several with comments explaining exactly why the two answers must not share a return value; the shared helper held the weak one, so the backends that reused code got the worse behaviour. **Fixed as `or_unrecognised_json(backend, found, container: Option<usize>, what, output)`**, which separates three answers a length alone cannot: `Some(0)` is an empty machine, `Some(n)` with nothing read is a schema change, and `None` — the container absent entirely — is the case every hand-rolled copy had already `let … else`-returned past and so could not express. Deleted with it: five private `unreadable`/`export_unreadable` builders, seven literal `found.is_empty() && !container.is_empty()` guards, and the `candidates[start..].join("\n")` copy of the full output that ran on every pipx and yarn listing. The winget and scoop readers keep their bespoke counts as counts — winget sums the packages across sources rather than counting sources, and scoop counts only *usable* apps, so an export of nothing but failed installs stays an empty machine rather than becoming unread. Also swept into the S51 family while here: `parse_dotnet_list_json`, `pixi_list_json`, `parse_winget_export`, `parse_scoop_export` and the onboarder's JSON arm now read through `json_document` too. Rule in II.27, reason in V.158. |
| **S55** | **FIXED 2026-08-09. The guard's ceiling was assembled by three callers and one passed `0`, so four packages and four ports under a limit of five were invisible to every guard call in the run.** `inspect_removals` took `also_removing: usize` and argued the case for it correctly in its own doc comment — *"a sync that drops three packages and three links removes six things, and a limit of five must see six"* — which left every caller holding a number. `verbs/sync.rs` passed `changes.total_remove()`, read after the TUI may have filtered the plan, and was right. `verbs/plan.rs` passed `package_pairs.len()` and was right. **`apply/firewall.rs:154` passed `0`**, because the count was simply not reachable from a struct holding `config`, `executor` and `registry` — the teardown had been wired into the guard three days earlier (`Y20`) and supplied the only value available at that call site. **A parameter a caller cannot answer is a parameter that gets answered with a zero.** Fixed as `guard::Reaping`, one value owned by the `App` — one invocation, one budget — read and written only by the `enforce*` family, so no caller passes a number at all. `Firewall`, `Extras`, `Leases`, `SyncEngine`, `ProfileManager` and `EphemeralShell` each hold it; `enforce`, `enforce_extras` and `enforce_deliberate` all record through it; a **refused** set is deliberately not recorded, so a removal that was never allowed cannot shrink the next phase's budget. **And the ruling that came with it (`Y20`, owner, 2026-08-09): two ceilings, not one.** `max_removals` stays packages-only at 20; `max_extra_removals` (new, 20) covers `link:`/`service:`/`setting:`/`shim:`/`schedule:`/`repo:` teardowns, a `dotfiles:` tree's files, and firewall ports. Neither spends the other's budget — one number made the stricter govern both, so tightening a perimeter made a machine less able to remove a package. `Q7` clause 2 is amended in the register: *whole command, not each phase* stands, *together* does not. `Objection::TooMany` now carries the setting name so a refusal sends the reader to the line they must change. Both answer to the same `--allow-mass-removal`. Layers swept: the in-code default, `GuardSettings`, `examples/preferences.toml`, `docs/spec/target-state.md` (four places), and the `grader_extras_guard_tests.rs` fixture, which set `max_removals = 1` for a five-*link* teardown and would otherwise have measured against the untouched default of twenty and passed for no reason. Rule in II.28, reason in V.159. |
| **S56** | **FIXED 2026-08-09. Two dispatches over the extras kinds matched on a `&str` and ended in a catch-all, and both catch-alls were wrong in the direction that leaves no trace.** `Statement::kind()` returned `Option<&'static str>`, which cannot be matched exhaustively, so both had a branch nobody had to justify. **`extras.rs`'s `undo_extra` ended `other => { warn!(…); Ok(()) }` — and `Ok(())` there means *the undo is done* to a caller that then rewrites the ledger without that key.** The resource is forgotten while still in effect, and since the ledger is the only record that Shall put it there, no later sync ever looks at it again; the `warn!` is the whole trace, at a level this same file's teardown comment already identifies as too quiet. `firewall:` reached that arm — `extra_key` keys every kind with a keyword — and was correct only by luck, because `Firewall::apply` closes undeclared ports by diffing the whole perimeter. **`in_effect` ended `_ => None`, and `None` means *unverifiable*, which places** — so a kind arriving there is re-applied on every sync for ever. Three kinds were answered and the rest arrived by omission, indistinguishable from the cases the function's own doc comment correctly names as unanswerable. **And `in_effect` already had the type in its hand**: it takes `stmt: &Statement` and then matches on a string split out of the *key* to pick the arm. Fixed as `ResourceKind`, ten variants with `Display`/`FromStr`, returned by `Statement::kind()`; both dispatches are exhaustive with no catch-all, and every do-nothing arm states its reason — `setting:` has no read-back command, `repo:` is answerable but deciding what a differing URL means is a ruling not a refactor (left `None` deliberately, written down), `schedule:` provisioning is idempotent, `firewall:` is reconciled as a whole perimeter, and `exec:`/`generate:`/`dotfiles:` are never keyed and are listed so the compiler keeps that true. One behaviour changed, in the safe direction: **a ledger row whose kind this build does not have is kept and reported rather than dropped**, because forgetting a row is the one outcome that cannot be undone. `ALL` is the only hand-maintained part of the type and a test drives every grammar keyword through it. Rule in II.29, reason in V.160. |
| **S57** | **FIXED 2026-08-09. The extras-ledger key was a `String` with three hand-rolled readers; it is now `ExtraKey { kind, subject }`, the only thing that formats one and the only thing that parses one.** The review that raised this counted **nine** re-splits of `Statement::key()` and called them one bug. Read one at a time they are **two key spaces wearing the same punctuation**, and that is the actual finding: `kind:subject` (the extras ledger — `extras_lock::split_key`, `guard::extra_removal_pairs`, `apply/extras::in_effect`) and `backend:name` (a package — `core/state::is_held`, `model/resolve::same_package`, `verbs/cleanup`'s OS-essential filter, `verbs/plan::scoped_by`, `verbs/packages::handle_info`). **Four of the five package sites are correct and were left alone**, and are listed here so the next reader does not re-open them: `is_held` compares halves in place and documents why (one allocation per declaration inside the planner's fan-out); `same_package` is set-math over package keys; `cleanup` splits a key `guard.rs` formatted three lines earlier; `scoped_by` is a *third* space again — the lock-file ledger's `adapters:backends.toml`. **The fifth was a real instance of the repo's own rule** (*one parser for `backend:name`; anything that splits on `:` and trusts the prefix is a bug*): `handle_info` built its `shall search …` hint with `package.rsplit(':').next()`, which for `web:https://example/x.deb` yields `//example/x.deb` — a suggested command nobody can run. It goes through `split_removal_target` now. **In the extras space the type is the fix**: `Display` writes the key, `FromStr` reads it, split at the **first** colon because a `repo:` subject carries its own — stated once instead of agreed on by five callers who never said why. `link_key` becomes `ExtraKey::link`, so the one string a `dotfiles:` tree and a `link:` teardown must both produce is one constructor rather than two builders held together by a comment. `extra_key` builds from `kind()` + `subject()` rather than from `Statement::key()`, which is the display form of a line and not a wire format. **The on-disk format is unchanged and the ledger still deserialises as strings**, parsed per row where used: parsing at load would make one unreadable row fail the whole file, and `S56` had just established that forgetting a row is the one outcome that cannot be undone. `guard::extra_removal_pairs` still carries an unparseable key through under an empty kind, because the count and the protection rules apply to it regardless — only its kind is unknown, and the guard does not dispatch on kind. Rule in II.30, reason in V.161. |
| **S58** | **FIXED 2026-08-09. Three managers described an upgrade-all verb in their config and were not registered `Upgradable`, so `shall upgrade` skipped them without saying so.** `winget` carries `upgrade --all --silent` and an `OutdatedProbe` that runs it; `scoop` carries `update *` and its own probe; `gem` carries `update`, which upgrades every installed gem. `choco` — the third Windows manager, in the same file, forty lines away — was registered. There is no error path: `as_upgradable()` answers `None` and every caller reads that as *this manager has no such concept*, the same answer `link:` gives. **The capability matrix asserted the loss**: `assert_caps(&reg, "winget", &[…])` listed five capabilities without `upgradable`, so the omission was pinned as intended on the day it was made. That is not a flaw in that test — a check written from the code can only say the code is what the code is. The unasked question was whether the *config* and the *registration* agree, and asking it found `gem` as well as the reported pair. **Two more registrations look like the same bug and are not, and the difference is the whole reason this is a list rather than a derivation.** `pip install --upgrade` takes package names and fails without them, so there is no upgrade-all to register; `bun upgrade` upgrades the bun runtime, not the packages bun installed, so registering it would make `shall upgrade` replace the user's toolchain while reporting it had updated their packages. **The tidy fix — deriving the capability set from `ManagerConfig`, as the custom-backend onboarder does — would have registered both**, and would additionally have given `bun` a `Searchable` whose parser is `|_| vec![]`, which is the inverse of the mistake that onboarder's own comment warns about: turning "not configured" into "no results". Shipped instead: the three registrations, plus `tests/a_configured_capability_is_a_registered_one_tests.rs`, which fails on any config/builder disagreement unless the manager is in `EXEMPT` with a sentence explaining it, plus a second test that no exemption has outlived its subject. Both scans read the two spellings this file uses (a struct literal and a `cfg.field = …` assignment after `base_config`) and are driven by an oracle over four planted bodies (II.23). Rule in II.31, reason in V.162. |
| **S59** | **FIXED 2026-08-09. Two of the four atomic writers had no `fsync`, and one of them is what writes the machine.** `utils/file.rs` opened with *"There were two of these… so there is now one."* There were four: `atomic_write` (flush + `sync_all`), `CommandExecutor::write_atomic` (**neither**), `CommandExecutor::write_secret` (flush, **no sync**), and `InstalledListings::save_to_disk` (neither, deliberately — it is a cache). **A rename is atomic against a concurrent reader and says nothing about power loss**: the directory entry can reach the disk before the bytes it points at, leaving a file of the right name and zero length. `write_atomic` is what writes systemd units, `link:` targets and every backend's state file, while `registry.json` and the WAL go through `persist` — so a crash after a sync kept Shall's record of what it did and lost the thing it did, which is the state `heal` is least able to reason about. **The fix is not one writer, because two of the differences are real.** There are two preview policies and both are correct: `persist` must not touch the config repo during a dry run (print *would write …*, stop), and the executor must make a machine write *visible to the rest of the preview* (divert into the VFS) or every multi-step dry run reads back the wrong thing. What was never legitimately plural is the durability, now `utils::file::durable_write` — dir, temp, write, flush, `prepare`, `sync_all`, rename — called by all three product writers. `prepare` is the permission hook and runs **before** the rename, because a `chmod` afterwards leaves readable plaintext at the target path for however long it takes and T5's argument is that "however short" is not an argument. `save_to_disk` keeps its own and the reason is recorded: a torn cache file is a miss, an fsync per listing is a disk barrier on the read path, and its pid-suffixed temp name exists because *the rename is only atomic per writer*. **The comment is replaced by a scan**: `tests/a_writer_that_reaches_the_disk_goes_through_one_tests.rs` walks `src/`, ignores `#[cfg(test)]`, and fails on any rename-into-place outside two allowed files, each carrying the sentence saying why — checked to still be needed, and driven by an oracle over a planted offender, a planted innocent and a planted test-only offender (II.23). A paragraph asserting a singleton was true when written and nothing re-derived it; that is the whole finding. Also corrected: `verbs/plan.rs`'s *"Every ledger these commands write goes through `utils::file::persist`"*, which is true of those commands and false read as a claim about the program — it now says which scope it means. Rule in II.32, reason in V.163. |
| **S60** | **FIXED 2026-08-09. Rollback's two arms followed two rules, and the register recorded the closed ruling that said otherwise.** `U41` (2026-07-27) ruled that both arms compensate. `LX-3` (commit `e9a6ac4`) then changed the *install* arm — `Prior::Absent` stopped being permission to remove, on the good argument that *"was not here before this run"* and *"is not wanted now"* are different facts and the manifest holds the second. **Nobody told `decisions.md`**, so the register showed `U41` as `ANSWERED` unamended while the code ran an asymmetry: an install that succeeded and is still declared stands, but a removal that succeeded of something still undeclared was reinstated — handing the next sync the same work to do again, which is the un-convergence the install arm had just been fixed to avoid. **The owner's argument for symmetry, verbatim: *"we could have figured it out the same way we know to delete it: it's not there."*** Exactly so, and it is the same set: a `Remove` node exists because the planner found the package in `present − desired`, and its absence from the plan's `Install` nodes is still true when the rollback fires. Both arms now call one function, `plan_intends_present`, so the symmetry is a property of the code rather than of two comments agreeing. **What it costs is stated rather than buried**: a package the user had, that a reconciling run removed, stays removed after a failed transaction — accepted because generations and snapshots are the durable put-it-back, a restore point is taken before every sync, and the WAL records the removal without the version, so the reinstate could only ever come back at whatever is newest. A durable `Prior` is **deferred, not rejected**. **Two scopes are exempt, and this is where the fix nearly went wrong**: gating on `declared` being non-empty would have been silently correct for `sync` and silently catastrophic for `rebuild`, whose *down* phase is a removals-only graph — "not in the set" would have meant "leave every declared package deleted". `GuardScope::reconciles()` is the discriminator, exhaustive over all twelve, with the two exceptions written out: a rebuild's removal phase is the first half of a reinstall of declared packages, and an `uninstall` was typed by a person. `U41` is amended in the register with the ruling, the cost, and the deferral. Rule in II.33, reason in V.164. |
| **S61** | **FIXED 2026-08-09. The two files a user pipes from the internet documented an option nothing read and read one nothing documented; the release job had never run; and a suppression was addressed to a linter nobody had installed.** Six findings in the shipping surface, each small and each in a place where small is expensive. **(1) `SHALL_BIN_DIR`** was in `install.sh`'s header list — the only interface a piped installer has — and appeared nowhere else in the repo. It is **wired in**, not deleted: cargo installs into `$root/bin`, so a directory ending in `bin` becomes a `--root` of its parent and anything else gets a staged install and a copy. Mirrored into `install.ps1`, which had neither. **(2) `SHALL_REF`** was read by both scripts, commented at length in both, and listed by neither. Both header lists now name all four variables and every name in them is read. **(3) `release-check.sh:71`** tested `rustup toolchain list | grep -q "^$MSRV"`, and an empty `$MSRV` makes that `grep -q "^"`, which matches every line — so a `Cargo.toml` whose `rust-version` had been renamed or requoted would report GO having run `cargo +"" check`. The `.ps1` twin had the explicit guard; the `.sh` did not, which is the twin-divergence shape again. **(4) The crate count**: `Cargo.lock` holds **448**, and six places said 380 or 452 — in a repo that wrote a 226-line script because two files tracked one number by hand. All six corrected. **(5) `# shellcheck disable=SC2086`** in `docker/integration/run.sh` and `scripts/harness-mutation-test.sh`, for a linter nothing ran: two suppressions addressed to nobody while every other shell defect in eleven scripts went unmeasured — and the harnesses that decide pass/fail for every backend *are* shell, where a quoting bug is a wrong verdict. A `shell` job runs `shellcheck -S warning` in CI (`warning`, not `style`: a gate that arrives red on cosmetics is disabled in week one), both release scripts run it softly, and `grade6_gate_parity` gained the row that keeps the three in step. **(6) The `release` job had never fired** — its only trigger is a `v*` tag and no tag exists, so the one step between a green build and a user downloading a binary was the least-tested thing in the repository, and `install.sh` compiles 448 crates on a stranger's laptop precisely because there is nothing to download. It now also runs on `workflow_dispatch`, where it downloads the same artifacts, asserts the binaries are present — a check the tagged path never makes, because `action-gh-release` fails on an empty glob only after the tag exists — and stops without publishing. Rule in II.34, reason in V.165. |
| **S62** | **RULED 2026-08-09 (`Z1`). Shall is `MIT OR Apache-2.0`.** There was no `LICENSE` file and no `license` key, which under the Berne Convention is *all rights reserved by default* — so every user of the install script held a copy they had no licence to. Shipped: `LICENSE-MIT` and `LICENSE-APACHE` at the root, the SPDX expression in `Cargo.toml`, a `## Licence` section in the readme, and **two deletions that were only ever placeholders for this answer**: `publish = false`, which stated today's truth rather than a preference, and `deny.toml`'s `private = { ignore = true }`, which existed so that gate would not answer the owner's question by implication. Both halves of the expression are already in `deny.toml`'s allow list, so Shall's own crate now answers the same licence gate every dependency does. |
| **S63** | **FIXED 2026-08-09. Removing a `setting:x@scope=system` line reset the USER key and reported success.** `SettingInstallable::remove` hardcoded `Scope::User`, under a comment that stated the cause and accepted it — *"Scope is not carried on a removal (only names are), so this resets the store's default scope — which is where an unscoped declaration wrote, the case that exists today."* Both halves of that are true and the conclusion does not follow: the case that exists today is not the only case the grammar accepts, and `@scope=system` is a supported spelling with a refusal written for it at apply time. The machine-wide value stayed in place while `sync` said the line had been undone. **The scope rides the ledger key now** — `setting:org.gnome.desktop/theme@scope=system` — because by teardown time the line that carried `@scope=` is gone and the ledger is the only record left. An unscoped key still means the store's own default, which is exactly what every row written before this said, so nothing on disk changes meaning. A store with no machine-wide commands cannot have written a machine-wide value, so that combination skips rather than falling back to the user key — the fallback being the bug. |
| **S64** | **FIXED 2026-08-09. Four one-line families, each in a place where the line was load-bearing.** **(1) Every `shall install` rewrote a CRLF module to LF in full.** `str::lines()` drops the carriage return and the rewrites rejoined with `"\n"`, so one added package became a whole-file diff and every later `git blame` pointed at the run that touched the line endings — in a grammar that accepts a BOM *because that is what Notepad writes*, and Notepad writes CRLF. Two halves of one courtesy, one delivered. One `rejoin` helper decides from the file's own bytes, at all three rewrite sites; a file with no newline takes the platform's. **(2) `shall why` resolved the whole configuration once per match.** `declarations_of` built a fresh `StateResolver` and called `resolve_model()` *and* `resolve_vars_with_origins()` inside the loop, so a name two backends carry read every file you own twice — in a read command whose only job is to answer from those files. Resolved once, before the loop, as a `ResolvedConfig` the function now takes. **(3) `md5` was a direct dependency for one cache key**, beside `sha2`, in a tool whose pitch is being careful what gets installed. The folder name is a truncated SHA-256 now and the dependency is gone. **(4) Three test binaries justified a source scan with *"`verbs/` is private to the binary"*** — `src/lib.rs` says `pub mod verbs;` and `verbs_are_reachable_tests.rs` imports through it. The scans are still right; the reason was not, and a wrong reason is what gets a correct test deleted by the next person who checks it. Each now says the true reason: the claim is about *every* call site, and exercising one proves nothing about the rest. |
| **S65** | **FIXED 2026-08-09. The installed listing was cloned in full to read one row out of it, once per declared package.** `InstalledListings::once` handed back an owned `Vec<Package>`, and `info` is list-then-find in thirteen backends — so every `info` call cloned the whole listing and did a linear find. Measured shape: a 256-line winget config against a 280-package listing is **~71,680 `Package` clones and 256 mutex acquisitions** to answer 256 questions. The memo holds `Arc<Vec<Package>>` now and `Queryable::installed_listing` is the read-only door; `list_installed` keeps its owned `Vec` for the callers that consume one, so the change is a new door rather than a migration. **The review's proposed fix was different and would have been wrong**: it wanted `installed_sets` to return `HashMap<name, Package>` and `spec_is_missing` to look packages up in it instead of calling `info`. That skips the matching rules each `info` carries — `generic.rs` matches choco/scoop/winget **case-insensitively** and accepts winget's bare moniker for a vendor-qualified id, `go.rs` matches a module path by its trailing segment — and it skips the properties `spec_is_missing` reads for `@channel` and `@classic`, which come from `snap info` and are not in `snap list`. A map lookup would have made every `winget:jq` declaration read as absent and reinstall on every sync. Same win, no semantics moved: the clone was the cost, not the call. Also fixed: `apply_scope_filtering` returned `desired.clone()` when there was no scope — the whole-machine sync deep-cloning the entire desired map to change nothing, ~1,500 allocations at 298 declarations — now a `Cow`; and `resolve.rs`'s `reached.statements.iter().cloned()`, which deep-cloned the whole configuration so a loop could own it, now destructures (`of()` reads only `scopes`, so the two halves separate cleanly). |
| **S66** | **FIXED 2026-08-09. The cargo cache key omitted the target it is keyed on.** `${{ runner.os }}-cargo-${{ hashFiles(…) }}`, with `matrix.target` used in the very next line's path. `macos-13` (x86_64) and `macos-latest` (aarch64) are both `runner.os == macOS`, so the two share one entry — and `actions/cache` does not save on an exact-key hit, so whichever Apple target loses the race restores the other's artifacts, rebuilds cold, and never writes its own. Permanently, on every run since the matrix gained a second Mac row. One `${{ matrix.target }}`. |
| **S67** | **FIXED 2026-08-09. `tests/` was 101 targets, each fat-LTO-linked against a 100k-line crate under `codegen-units = 1`, and `target/` reached 194 GB and filled a 944 GB disk mid-build.** Thirty-six of those binaries never call the library API at all — they only spawn `CARGO_BIN_EXE_shall` — and linked it for nothing. **The suite had already paid this cost once and written it down**: `mock_providers/mod.rs` opens by recording that a top-level `mock_providers.rs` became *"a 716 KB binary containing zero tests… compiled nineteen times"*, and that making it a directory fixed it. This is that fix at the scale of the directory: `autotests = false` plus one `[[test]]` target whose root, `tests/main.rs`, lists every file as a module. **Nothing moved and nothing was deleted** — every file keeps its path, its filename-as-a-sentence and its doc comment — and `mock_providers` is declared once instead of nineteen times. Measured: `target/` **194 GB → 19 GB**, 101 link units → 1, and the suite runs as 423 tests in one binary. Two things had to change and both are improvements: the two files opening with `#![cfg(...)]` carry the gate on their `mod` line instead, so off-platform they are not compiled at all rather than compiled to nothing; and **two lines only ever compiled because their private binary happened not to load `smartstring`** — `rhai` pulls it in and it adds `impl Add<SmartString<_>> for String`, which makes `String + &String` ambiguous and stops deref coercion being attempted the moment those impls are in the unit. Both now use `format!`, which is what they should have said. The cost of the arrangement is that a file not listed in `tests/main.rs` never runs, and it is payable only because `every_test_file_is_in_the_suite` compares the directory against the module list and fails when they disagree. Rule in II.37, reason in V.168. |
| **S68** | **FIXED 2026-08-09. One `Fixture`, and the three ways sixteen copies of it had drifted.** The same twenty-five lines — a temp root, `shall init`, `run(&[…])`, join stdout and stderr — were written out in **sixteen files**, and no two of them agreed: **`current_dir` was set by 3 of 16**, so thirteen ran the binary in the *repository root*, where a stray `shall.txt` is a project-local shell manifest the product reads; **`HOME`/`USERPROFILE` was set by 3 of 16**, so thirteen resolved `~` to the developer's real home — a test placing a `link:` at `~/.vimrc` was writing to their actual dotfiles, and passing or failing on where the checkout happened to sit relative to `$HOME`; and **`cfg()` existed in 11 of 16**, with the rest spelling `root.join("config")` inline. `tests/harness/mod.rs` is the union, and the union is the correct one in all three cases — a test process that cannot see the developer's home directory or the repository's working directory is the only kind whose result means anything. **Net −616 lines across 17 files**, and thirteen tests are hermetic for the first time. Three helpers that were byte-identical in two or three files each — `decl`, `write_module`, `seed_ledger` — moved with it. **Per-file setup was kept, not flattened**: five files did real work in their `new` (creating `tree`/`src`/`dest`, appending `use extras` to the profile, seeding link declarations) and each keeps it as a free `setup(name)` built on `Fixture::new`. The first pass of this conversion silently dropped those five, which is the same failure the change is about; it was caught by diffing every deleted `new` against the shared one before running the suite. Rule in II.38, reason in V.169. |
| **S69** | **FIXED 2026-08-09. Nine scanning gates each wrote out the same four assertions about their own exemption table, and three of the nine were missing the one that matters.** The shape is identical everywhere: a predicate walks the tree, a `const` table excuses the sites that must stay, and the gate asserts that the walk read enough to have found anything, that the predicate still matches something, that nothing found is unexplained, and that no entry has outlived its subject. **Written nine times, the first of those four went missing three times** — which is `S48` from the other end: a scan whose predicate had stopped recognising its subject passed by finding nothing, and read as a green gate for months. `tests/ledger/mod.rs` owns the four now, and **the floor is not optional**: `Ledger::audit` panics on a floor of zero rather than skipping the check, because *“I could not think of a floor”* is exactly how the three vacuous copies came to be written. Its own oracle drives each of the four over a planted input built to violate it, plus the two that are easy to get subtly wrong — a raised reason floor really being the one enforced, and a reason measured in characters rather than bytes. **What did not move is as much of the point as what did.** A site's own knowledge stays beside its table: `os_native_argv`'s *an exemption must not contradict the row three lines below it* (the `helm` case) runs **before** the audit, because the shared stale check would otherwise pre-empt it with a vaguer sentence; `backend_is_data`'s *a reason may be long and still be a schedule*, `lifecycle_coverage`'s *the exempted name must be a backend at all*, and `a_configured_capability`'s *the exempted name must be a registration that exists* all stay, each with a line saying which failure the shared check would misreport. **Four tables were scoped and deliberately not converted, because they are not exemption tables**: `wal_enumeration`'s `LEDGER` accounts for every mutation site rather than excusing any, and carries a call count and a mechanism per row; `adapter_tables`' `WRITTEN_ONCE` lists patterns that must appear exactly once rather than sites excused from a rule; `unknown_backend_family`'s `NOT_A_PACKAGE_SPEC` is a classification (*this verb's argument is a profile name*) with no scan behind it, so there is no found-set to subtract from; and `named_commands_exist`'s tables are pinned to file and line rather than to a site name. Forcing those into the helper would have been uniformity bought with meaning. **Three conversions changed behaviour for the better**: `a_configured_capability`, `output_is_sanitized` and `a_plan_installs_only_declarations` each subtracted their exemptions *inside the walk*, so the finding set arrived with the excused sites already gone and the gate could not tell a live exemption from a dead one — the subtraction happens in the ledger now, which does it in both directions. Net −121 lines across ten files. Rule in II.39, reason in V.170. |
| **S70** | **MEASURED AND REFUSED 2026-08-09, with one deletion and four renames taken.** The review's compaction item D closed with *five subsumed binaries — `dag_test` (155), `integration_test` (178), `e2e_tests` (216), `shell_lifecycle_tests` (180), `help_map_tests` (162) = 891 lines*, to be deleted after folding two assertions into stronger siblings. **Read one test at a time, seventeen of the eighteen are live coverage with no sibling.** `test_parallel_task_isolation_wiring` is the only measurement in the repository of the *lock's granularity* — different backends overlapping, the same backend serialising — and it was itself rewritten out of a version that proved neither half. `MetricsCollector` is asserted in `integration_test` and **nowhere else**; so are the validator's five refusal shapes, the template-drift plan, the four `shall shell` lifecycle properties, and bundle→restore without git. `help_map_tests` is not subsumed by `named_commands_exist_tests` either — **it says so in its own comment, and the comment is the reason it is safe**: its exemption list may excuse the English word *undo* precisely because the other gate is what decides whether a command is real. Deleting it would remove the *every command appears in the map* direction, which the second gate does not check. **One deletion was taken and it is the one that was written down**: `e2e_tests::test_e2e_sync_flow_hermetic` — one `brew:neovim`, install-only, no second run — against `a_machine_converges_tests.rs`, whose header opens by naming it as the gap it exists to close and which runs the same path forward, backward and forward again. **What was real in the item was the naming.** Those five files were the last of the pre-v7 `test_*_wiring` style, with `// ====` banners, *"A+ Test Infrastructure"* comments, and numbered-step narration (*`// 4. Initialize the Transaction`*) of exactly the kind V.42 bans. Four are renamed into the directory's filename-as-a-sentence convention with their test names and headers rewritten to state the property; the narration is gone; every assertion is unchanged. **The link-unit argument, which was the item's actual weight, was already collected by `S67`** — 99 targets became 1, so what remained here was line count against live tests, and line count is not a reason to stop asserting something. |
| **S71** | **FIXED 2026-08-09. Twenty-three backends were a `[[backend]]` row written in Rust, and the reason they were not one is that a TOML table had no way to name a function.** `fn register_npm`, `fn register_yarn`, `fn register_cargo` and twenty more were forty-line struct literals of argv and flags — `ManagerConfig { install_args: vec!["install".into(), "-g".into()], … }` — indistinguishable from the `[[backend]]` rows a user writes in `~/.config/shall/backends.toml`, **except in one field**: `parser`. A row could describe a shape (`ParserSpec`) but not name `ws_name_version`, so anything whose listing needed a real reader had to stay a function, and once one field forced Rust the other thirty came along. `src/parsers/named.rs` is the missing half — four resolvers (`installed`, `search`, `probe`, `names`, matching the four call sites in `build_capabilities`) over sixty-eight named readers, each already a tested function with a fixture behind it, plus a `NamedParser` that carries the backend name to the reader. **Six new row fields name one** (`reads`, `searches`, `outdated_reads`, `machine_list_reads`, `essential_reads`, `depends_reads`); five more (`os`, `upgrades_all`, `install_source_option`, `upgrade_reinstall_args`, `repo_list_shape`, `search_source`, `extra_probes`, `property_probes`) carry what else the twenty-three needed. `registry.rs` goes 4,852 → 4,070 lines; `builtin_backends.toml` is 444, and the *whole* of `helm` is nine lines of it. **The argv assertions did not move.** Sixty-three `ArgvCase` rows still name a registrar function, so the test module defines twenty-three shims through `rows_as_registrars!` — each one line calling `register_builtin_row`, which ignores the OS gate so `emerge`'s argv is still checked on Windows. **`register_builtin_backends` runs first in `create_default_registry`**, so a hand-written registration wins a collision and `no_backend_is_both_a_row_and_a_registrar` fails when there is one. **A row can be wrong in four ways a function could not, and each has a gate that was watched to fail on a planted defect**: a misspelled reader name is not a load error but an `Option` falling back to the described parser, so `reads = "ws_name_versoin"` would compile, register, and report an empty machine (Q40's class through a new door); `search_args` without `searches` gets `NamedParser`'s empty-vector closure and answers *no such package* to everything from a backend advertising `Searchable`; `list_args` with neither `reads` nor `parser` gets one-bare-name-per-line and reads every versioned listing as total drift; and `install_source_option` is the same fact as `capability::install_source_key`, written in two files, where the row's half appends to the argv and the grammar's half decides whether the user may write `@url` at all. Rule in II.40, reason in V.171. |
| **S72** | **FIXED 2026-08-09. A reader served eight managers on one manager's output, and asking the other seven found four bugs in an afternoon.** `ws_name_version` covers cabal, spack, pub, krew, helm, guix, luarocks and uv, and the only input it had ever been run against in this tree was `NAME VERSION` / `foo 1.2.3` / `bar 0.1.0 some-desc` — seven words typed by hand and labelled `helm`. **`[backend.fixture]` is the column that makes that impossible**: a row carries the bytes its manager printed, what the row's own reader must produce from them, and a `source` saying where the bytes came from; `every_fixture_reads_the_way_its_row_says_it_does` runs one against the other through `parser_for`, the *same* function `build_capabilities` resolves the live parser with. Twenty-two of the twenty-three rows can list and all twenty-two now carry a fixture; **fifteen were captured from containers** (node, python, composer, haskell, ocaml, elixir, dart, rust, nim, pixi, alpine+luarocks, alpine/helm) and seven are written from upstream documentation and say `UNVERIFIED` in their `source`, under a ratchet that may only fall. **Four defects, each found by real bytes and none by reasoning.** *(1)* `uv tool list` prints `ruff v0.16.2` then `- ruff` for each executable, and the second line read as a package named `-` at version `ruff` — every uv machine reported twice the tools it had; `uv_tool_list` skips the executables and strips uv's decorative `v`, which an `@version=` pin could never have matched. *(2)* `cabal list` on a container with no config writes three lines of chatter to **stdout** before the packages, and `Config file path source is default config file.` is not prose by `is_prose_line`'s rule (it does not start with *no*/*nothing* and its first word is not a number), so it became the packages `Config@file`, `Config@file` and `Writing@default`; `cabal_list` requires the second column to start with a digit, which Haskell's PVP guarantees. *(3)* nimble 2 prints `nimpy  [(version: 0.2.1, checksum: …)]` where nimble 0.13 printed `[1.0.0, 0.9.0]`, and the reader recorded the version as `(version: 0.2.1` — a string no pin matches and `shall list` prints verbatim. *(4)* **The family fix**: `helm plugin list` with no plugins prints its header and stops, and *ten* readers in `ecosystem.rs` dropped a header row from the packages while leaving it in the candidates — so `or_unrecognised` saw one data line yielding nothing and refused the entire listing. A manager with nothing installed is not a manager nobody can read; the header check moved into `is_noise_line`, which is the one place all ten go through. **The free riders were wired in, not deleted.** `parsers/utils.rs` had zero callers: `split_columns` is now `ParserSpec::Columns { quoted = true }`, the shape a user's row could not express for a Windows manager that prints `"7.3.4 (x64)"`, and `extract_version_bracketed` replaced the hand-rolled `trim_matches('(' \| ')')` in `mas` and `gem` — both keeping their old fallback, because a wrong version is drift and a dropped package is a removal. `parsers/pkgsrc.rs` was **measured and kept**: the review calls it "74 lines that now delegate", and it is four lines of delegation, fifty lines of tests, and two live call sites — its own header already explains that the backend *label* is what differs and a shared label would put `pkg`'s prefix on every pkgsrc package. Rule in II.41, reason in V.172. |
| **S73** | **FIXED 2026-08-09. Three backends download and three wrote the same removal, and `appimage.rs`'s own test header said so before anyone acted on it** — *"its removal is `web.rs`'s removal with the D5 handoff taken out — same state file, same two deployed paths, same re-insert-on-failure rule."* The four steps are identical in `github.rs`, `web.rs` and `appimage.rs`: hand the system manager back what it owns (D5), delete the deployed paths, drop the cached download, and **put the record back if anything failed** — because a file still on `PATH` with no state row is drift no `sync` can see. `artifact/teardown.rs` owns them now, behind a `Deployed` builder whose methods encode the three conventions the records share: the empty string means *there is no such path*, an `installed_by` without a `system_package` is not an owner, and a cache entry is named by a URL's last segment. **The record shapes did not move** — `github:` folds several artifacts into one `Deployed` because one release can deploy several, which is the only real difference between the three. **One behaviour change, and it only adds**: the three failure sentences said *"still installed"* (github, appimage) or *"still on disk"* (web), and the shared one says both — *"still installed and still on disk"* — because a `.deb` handed to dpkg is in a package database as well as on the filesystem, and neither half of what the three used to report is dropped. **The cache is now dropped only when the removal succeeded**, which `github:` and `web:` already got right in a different order and is now true by construction: a cache emptied beside a file that would not delete makes the next install a fresh download for nothing. **The identity rule got a gate rather than a comment.** `appimage:` once reported the *basename* while keying its state by the *URL*, so `info(url)` never matched its own record: every declared AppImage read as absent, `sync` re-downloaded all of them on every run for ever, and a removal could never find the row it meant to delete. `a_downloaded_artifact_is_named_by_its_key_tests.rs` asserts, per backend, that whatever `fetch_installed` calls a package is a name `remove` finds — written against real state files on disk, and watched to fail with the basename bug planted back in. **`pip_search.rs` stopped being a bespoke capability**: `PipSearchable` was an `impl Searchable` bolted onto pip's registration, so *"this backend's search is an HTTP call"* was a thing only Rust could say — `node_registry.rs` had the identical shape and a `SearchSource::NpmRegistry` variant, and pip had none. PyPI is now `SearchSource::PyPi`, pip registers a plain `GenericSearchable`, and a row can write `search_source = "pypi"` for the same reason it can write `"npm_registry"`. The parser is unchanged and now takes the asking backend's name rather than hard-coding `pip`. Rule in II.42, reason in V.173. |
| **S74** | **FIXED 2026-08-09. Six places asked a yes/no question and each decided for itself what happens when nobody is there to answer.** The three steps are mechanical — honour `--yes`, check `stdin().is_terminal()`, `dialoguer::Confirm::new().default(false).interact()` — and the middle one is not: **a confirm has three outcomes, not two.** Yes, no, and *there is no terminal*, and the third is a policy about the machine that six copies each got to invent. `core::prompt::confirm` takes it as an argument, in the two shapes the six actually used: `Unattended::Refuse(&str)` for anything that changes the machine — orphan removal, rebuild, placing dotfiles outside `$HOME`, applying a stale captured plan — and `Unattended::Decline(&str)` for an offer the run survives without, which is bootstrap installing a missing package manager and prereq running a setup command. Both sentences stay at the call site, because a generic one names no verb and no flag. **Two gates moved with it.** `prompt_guard_tests` scans for `.interact()` and demands an `is_terminal` above it; six sites collapsing into one took its count from 11 to 7, so its floor came down — and `only_one_place_asks_for_a_yes_or_no` is the successor, because *there is exactly one confirm* is a stronger statement than *every confirm is guarded*. `grader_refusal_exit_code_tests` scans for the word *refusing* and demands `Error::Refused` within eight lines; `Unattended::Refuse` is now the second construction it recognises, and it is a real one — `confirm` is the only reader of it and `a_refusing_prompt_with_nobody_there_refuses_by_name` matches on the variant rather than the message to prove the conversion. **`dotfiles.rs` moved its `format!` inside the constructor** rather than binding it eight lines earlier, which is the sort of thing a lookback window makes matter and is better code anyway. Rule in II.43, reason in V.174. |
| **S75** | **FIXED 2026-08-09. `--json` reached twenty handler signatures as a bare `bool`, and a `preferences.toml` that said `dry_run = true` was flattened to false four lines under a comment promising it "counts too".** Two findings, one shape: a boolean carrying a decision it cannot name. **(a) The flag.** `handle_sync(app, locked, upgrade, json)` is three positional booleans in a row, `handle_install(app, packages, json, temp, into)` the same one argument along, and four call sites wrote `handle_sync(app, false, false, false)` — transposing any two compiles and converges the machine to the other answer. Every printing site then re-derived *a person is reading* by negating *a machine is reading*: `!json`, which is the dialect both of this file's earlier `--json` defects were written in. `core::Output` is `Human` or `Json`, converted once by `Output::from_json_flag` in `main`'s dispatch and passed down as itself; `!json` is `out.is_human()`, an affirmative. The two survivors of the same class went with it: `handle_sync` takes a `SyncMode { locked, upgrade }` so the pair is named at every call site, and `insight.rs`'s `as_json` — a third spelling of the same bool — is an `Output` too. `a_verb_is_handed_a_reader_not_a_flag` fails on the twenty-first `json: bool`, exempting only the two ends of the flag's life: `args.rs`, where clap parses it, and `output.rs`, the single conversion. **(b) The overwrite.** `merge_cli_overrides` took six positional `Option<bool>`s and `main` handed it `Some(cli.dry_run)` — but `cli.dry_run` is a clap flag, so *absent* arrived as `Some(false)`, and `Some(false)` overwrites. `dry_run`, `yes`, `allow_mass_removal`, `allow_mass_install` and `verbose` are all `#[serde(default)]` keys of `preferences.toml`: **all five were parsed, stored, and then erased before dispatch**, so setting any of them in the file did nothing at all. None of the five has a `--no-` counterpart, so an absent flag can only mean *the file decides*; the flags are plain `bool`s in a named `CliOverrides` now and only ever turn a setting on. The three tests walk the five as pairs rather than pinning `dry_run` alone — one of the five being noticed and the other four not is how this survived — and `a_setting_written_in_the_file_survives_a_flag_nobody_passed` was watched to fail, naming the field, with `self.yes = cli.yes` planted back in. Rule in II.44, reason in V.175. |
| **S76** | **FIXED 2026-08-09. Two predicates for "does this retention policy delete anything", four implementations of "delete this path", five readers of "does this line open a block", and two `Writes` enums — plus three helpers in `utils/file.rs` that nothing called, each of which was the good version of a pattern written out longhand elsewhere.** Compaction F, and three of the merges turned up live defects. **(a) A pin list was permission to delete.** `RetentionPolicy` carried `prunes()` (count or age rule on) and `is_active()` (count, age, *or* a non-empty `keep`), and `select_deletions` used the second: a `[retention.snapshots]` reading only `keep = ["known-good"]` — a pin list, and nothing else — turned deletion on, matched every other entry against a count rule of zero and an age rule of zero, and reaped all of them but the pin and the newest. Pinning is how a user says *keep this*; it can never be how they say *delete the others*. One predicate now, and the header no longer opens by naming three histories when two of them have been git's job for a phase. **(b) `force_remove` followed symlinks.** It branched on `path.is_dir()`, which resolves the link, so a symlink to a directory got `remove_dir_all` — emptying the *target* and leaving the link. `link:` deploys exactly that shape, and its two copies of the branch already had the `!is_symlink()` clause this one lacked. Fixing it exposed the sibling: on Windows a directory symlink cannot be deleted with `remove_file` *or* recognised by `symlink_metadata().is_dir()`, so `remove_deployed_path` — the async twin, which `link:` now calls — was failing there with `Access is denied`. Both go through `remove_by_kind` now, which tries the file form and falls back to the directory form, and the test that found it creates a real symlink and asserts the target's contents survive. **(c) `active`'s writer read a block header differently from `active`'s reader.** `profiles.rs` wrote `strip_prefix("when ").unwrap_or("")`, which makes any non-`when` header a `when` with an empty predicate — a gate that evaluates false and silently drops what it holds — while `gated.rs`, reading the same file, refuses such a header outright. `grammar::block_header` and `grammar::when_predicate` are the two questions now, asked in one place by all five callers including the REPL's `when`. **The wired-in dead code.** `ensure_dir` names the directory it could not create (`Access is denied. (os error 5)` with no path, in a program that has just touched a dozen), gained an async twin, and is 24 call sites; `append_line` learned not to glue onto a file that ends mid-line — a hand-edited `.gitignore` would have become `target*.shall-backup` — and now writes that file for `git init`; `read_lines_filtered` reads it, so a commented-out pattern is correctly not present; `force_remove` is `scheduler::remove_generated`, which keeps its own sentence about a timer left armed. `Journal::new` is what `App` calls when nothing has placed the registry, and the rule that the WAL lives beside it moved onto the constructor from a comment above the caller that derived both paths by hand. `Journal::removals_of` is `leases` and `suspend`. `bundle.rs`'s private `Writes { ToDisk, Preview }` is `model::Writes { ToDisk, Planned }`, whose three filesystem effects now live on the type. **Not found: `md5`.** It is not a dependency and never was; `web.rs` carries a comment explaining that `sha2` was chosen over it, which is the opposite of a second hash implementation. Rule in II.45, reason in V.176. |
| **S77** | **FIXED 2026-08-09. Six of `harness-logic-test.sh`'s predicates never ran a script or entered a container — they read `ci.yml`, the release scripts, the Dockerfiles and the harnesses as text — and being written in shell cost them twice.** Once in the pipelines: `grep -c` prints `0` *and* exits 1 when it matches nothing, which is how the mutation gate came to report success in the total-collapse case its guards exist to catch, and MSYS grep normalises CRLF before matching, which made the CRLF gate blind on the one platform where the bug occurs — it needed a self-test of its own detector to know that. Once in when they run: at the end of a release script or in CI, rather than in `cargo test` beside the twenty-seven Rust gates that already read this repo. Five are now `tests/the_review_apparatus_is_rust_tests.rs` — orphan scripts, function-defined-before-called, CRLF endings, floor mounts, image identity — each with a floor against vacuity and each watched to fail with a defect planted: a gate name swapped out of `release-check.ps1`, a call inserted above its definition, a mount renamed, an `ENV SHALL_IT_IMAGE` falsified, an orphan script created, a CRLF script created. **The sixth was not ported, and finding that out is the finding.** Gate parity already had a Rust successor — `grader_gate_parity_tests::every_gate_ci_runs_is_run_locally_with_the_same_target`, written when the shell one was caught comparing basenames — and it is the stronger of the two, keying on the whole invocation rather than the script name. Porting the shell version would have made three implementations of one question inside the change whose whole subject is that. **The half that stays in shell is the half that cannot move**: `lift` pulls function bodies out of the harnesses and runs them, which tests the bytes CI executes in the interpreter that executes them. **The exemption list is audited too** — a `NOT_GATES` entry naming a script that no longer exists is a claim about nothing, which is how an exemption outlives what it excused. **Running the trimmed script found live drift**: the decision register held 176 ANSWERED / 2 BUILT / 0 OPEN while `decisions.md` and `SPEC.md` both said 174 / 3 / 1 and the index still listed `Z1` as OPEN, four days after it was ruled. Corrected in all four places. **Not found: a linter nothing runs.** The review's *"two files already carry `# shellcheck disable=` directives for a linter that nothing runs"* is stale — CI has a `shell` job running `shellcheck -S warning` over `scripts/*.sh` and `docker/**/*.sh`, and `release-check.sh` runs it too when it is installed. The SC2218 gate is kept anyway, because it runs in `cargo test` on a machine with no shellcheck, which is where the harness is being edited. Rule in II.46, reason in V.177. |
| **S78** | **FIXED 2026-08-09. Eight extension surfaces, and nothing in the program knew there were eight.** Owner request, the same day: a plugin system, *"almost like how in Lisp you can add your own"*. The surfaces already existed and already worked — `[[backend]]` teaches a package manager, `[[snapshot]]` a rollback provider, `[[secret]]` a decryptor — and every one already went through II.12's ledger. What was missing is that the list of them lived only in a person's head: eight paths on `Layout` (seven, actually — see below), eight readers, eight `warn!("ignoring adapters/x.toml: …")` lines, and no way to ask *what have I extended, and is it working?* **The costly failure is not a bad row, it is a row nobody knows was dropped.** `[[backends]]` where the reader wants `[[backend]]` is valid TOML describing a table nobody opens: no parse error, no warning, and a `mymgr:` line that fails much later with a message about an unknown backend. So `app::adapters` is one table of eight `Surface`s, `survey()` asks the same two questions the readers ask in the same order — does the ledger approve these bytes, does the file hold rows of this kind — and `shall adapters` prints surface, row syntax, standing and **rows in force**, which is the column that catches the typo. `--json` for a script; a surface name narrows to one; a name that is not a surface is refused with the eight listed. **`firewall:` had no `Layout` accessor**, reading `adapters_dir().join("firewall.toml")` inline, so a table derived from the accessors would have had seven rows and no way to notice the eighth — `Layout::adapter_file` is the one join now and `no_adapter_file_is_opened_by_hand` keeps it that way. **The eight bespoke warnings are `adapters::cannot_use`**: a serde message on its own (*"missing field `name` at line 4 column 1"*) says which line and nothing else — not which of eight files, not what a row of that kind looks like, not that the rest of the file is inert, not that a command exists which says all three. **Four gates, each watched to fail with a defect planted**: a ninth surface read by a file, a reader writing its own sentence again, the readme dropping a surface, and a repo with no `adapters/` directory at all — the case almost every machine is in, where a survey that complained would be worse than none. **What was deliberately not changed:** a malformed adapter file still degrades to a warning rather than refusing the run. Turning it into a refusal would be a behaviour change on a path where a working machine is mid-`sync`, and it is not this task's to make. Rule in II.47, reason in V.178. |
| **S79** | **FIXED 2026-08-10. Three ceilings of twenty permit fifty-seven changes, and nothing in the program objected to fifty-seven.** `N8` was recorded as an unasked question; it had been asked, under `Y20`, and answered the same day — searched for in the `N` series and living in the `Y` series, because it arrived through the removal-guard work rather than the firewall work. What `Y20` did leave open is what the owner ruled here: **a ceiling per category and one over all of them**. `max_port_closures` (20) splits ports out of `max_extra_removals`, because the run that first declares a perimeter closes far more ports than a settled machine tears down resources, and under `Y20` declaring a firewall spent a budget that had nothing to do with firewalls. `max_total_changes` (0, off) counts **everything one command changes** — installs and upgrades, every removal, resources written, ports opened and closed — which is why `enforce_additions` exists at all: it has no ceiling of its own and refuses nothing on its own account, and without it a total would have been the removal ceiling with a longer name. Off by default because any non-zero default refuses syncs that ran yesterday on machines that never asked, and would be turned off before it caught anything. **Three things the change turned up.** `Reaping`'s `already: usize` parameter — the `S55` shape, one level up — was still in `inspect_removals`, so the preview in `verbs/plan.rs` hand-assembled the number the enforcer computes; it takes the ledger now, and `guard::preview_refusals` is the one implementation of *what will `apply` refuse*, asked in the engine's order over one ledger. A refusal named the first ceiling it hit, not all of them, so a set over two sent the reader to raise one number and meet the other on the next run. And `shall protected` printed `max_removals` alone — a third of the answer, with no way to tell it was a third — in both its human and `--json` forms. **The adapters half, ruled the same day.** A malformed `adapters/*.toml` still warns and skips mid-`sync`: the file is optional and a typo in it must not stop you installing a package. `check adapters` is the new ninth section and exits non-zero on any surface written and not in use, which is where being loud costs nothing. A sibling went with it: `check_approvals` justified itself in a comment claiming adapters *"block a sync loudly"* — they do not, they warn and skip exactly like an event hook does. Rules in II.28 and II.47, reasons in V.159 and V.178. |
| **S80** | **FIXED 2026-08-10. CI had not run a single step in ten commits, and the release job would have published one binary four times.** `S67` ended a step with `--test suite pty_tests::`; YAML read the trailing colon as a mapping key and GitHub refused the whole file. That does not fail a job — there is no job. It fails the *run*, in zero seconds, with nothing to open and no log to read, so ten commits landed reporting a local build/test/clippy as their verification, each correct about that and each wrong about CI. **The gate is `every_workflow_value_that_yaml_would_read_as_a_key_is_quoted`**, a text scan that says it is one: the repo has no YAML parser and is not acquiring one for this, so it checks the class the defect belongs to — an unquoted scalar ending in `:` or containing `: ` — and self-tests against the exact bytes that killed CI. **Then the release job itself, whose only trigger is a `v*` tag and which had therefore never executed.** A GitHub release asset takes the file's basename; all four build targets produce `shall` or `shall.exe`, so three of the four would have been overwritten by the fourth and three platforms would have downloaded the wrong architecture from a release page that looked complete. The binaries are renamed to `shall-<target>` before publishing, and the pre-publish assertion names all four targets rather than counting files — a count is what passes when two of them collide. **And the installers became installers.** Both headers promised *"the 30-second first run"*; both ran `cargo install --git`, which resolves 448 crates and compiles them under fat LTO on a stranger's machine, and both demanded a Rust toolchain *before* discovering whether a prebuilt binary existed — a package manager whose own installation required a compiler. They fetch the published asset for the platform first, in both twins, and fall back to the source build for anything unpublished, no downloader, or no network. Rule in II.34, reason in V.165. |
| **S83** | **FIXED 2026-08-10. The build matrix declared four targets and produced one, so `cargo test` had only ever run on Windows.** A base `rust: [stable]` above the `include:` gives the matrix exactly ONE combination, and GitHub merges an include entry into an existing combination whenever it overwrites none of the base values - so all four rows merged into that same job in turn and the last, Windows, won. Three consecutive runs produced a single `Build for x86_64-pc-windows-msvc`; Linux and both Macs were never compiled, while the release job asserts four binaries in `dist/` and the installers download them. The same shape as `S80`'s four assets all named `shall`, found the same way - by reading what a run actually did. Each row carries its own `rust` now, and `every_target_the_release_publishes_is_one_the_matrix_actually_builds` ties the targets named in the release step to the targets the matrix builds and refuses a matrix that cannot expand to one job per row, with its predicate shown failing against the shape that shipped. **And it was 24 minutes on its own** - `lto = true` with `codegen-units = 1`, which forbids parallel codegen by definition, are settings for a published binary and not for a gate; tag builds keep them, every other run gets thin LTO. **What the fix found:** seven tests that had never executed. Three `web` tests registered `dpkg -r fd` where a non-root Linux runs `sudo dpkg -r fd` (the rule is now `CommandExecutor::escalates`, asked rather than restated); a winget test fabricated `0x8A150001` as an exit code, which a Unix status cannot hold; two `cfg(target_os = "linux")` hermetic tests pinned argv from before `--` terminated options and before `purge` became opt-in; a service argv row had two branches for three init systems and `setting` had no macOS store at all. **And one real defect in the product:** `dnf` forgave exit 100 with no failure phrasing able to contradict it, so every dnf run ending on that code read as a success including one that did nothing - the `choco` 3010 defect, live on the second-largest Linux manager, invisible because `benign_exit_contradiction_tests` walked the *registry* and dnf does not register on Windows. Measured in the Fedora image: `Failed to resolve the transaction:`. The gate now walks `exit_policy::MANAGERS_WITH_A_POLICY`, so all eighteen are audited on every platform rather than whichever happen to register. |
| **S84** | **FIXED 2026-08-10. The nightly sweep had not evaluated one assertion since the suite was consolidated.** `Cargo.toml` sets `autotests = false` and declares exactly one test target, `suite`. Three nightly jobs still invoked `--test argv_drift_tests` and `--test terminator_probe_tests`, which cargo answers with `error: no test target named 'argv_drift_tests' in default-run packages` before running anything - so `Upstream argv drift` on all three platforms and the `tools` leg failed in about three seconds every scheduled night, and the upstream-drift sweep is the only thing in this repo that notices a package manager changing its flags underneath us. Five consecutive red scheduled runs, into a log nobody opens. The consolidation updated the one step someone was reading and missed the three that only run at 03:00. `every_test_target_ci_invokes_is_one_that_exists` now parses `--test <name>` out of the workflow and checks it against the `[[test]]` targets in `Cargo.toml`, with `the_test_target_scan_sees_the_line_that_was_broken` showing the predicate failing against the shape that shipped. **And the fix broke the workflow on the way in:** `--test suite -- argv_drift_tests::` contains `: `, which YAML reads as a mapping key - the identical defect that left CI unexecuted for ten commits earlier the same day. `every_workflow_value_that_yaml_would_read_as_a_key_is_quoted`, written that morning, named both lines and the reason before the push. **Second half: gentoo asserted a premise the run had already contradicted.** The journal audit requires the write-ahead log to be non-empty on the stated grounds that *"sixteen sections of installs and removals have run above this line"*; gentoo runs `SMOKE_ONLY=1` because Portage builds from source and installs nothing by design. Soft-skipped when the log is empty *and* the run is a smoke run, hard everywhere else - the check itself is sound and has survived a `shall` that failed everything. **The pattern, named because it is now four in one day:** 16f measured the machine rather than the section, 16e blamed a stale stamp for a legitimate wait, fedora was killed by a timeout equal to its own wait budget, gentoo asserted installs that never ran. A check whose sentence does not describe its failure is worse than no check: it sends the reader to the wrong subsystem. |
| **S85** | **FIXED 2026-08-10, once `Q53` was ruled. Locking a `brew` package made it permanently uninstallable.** The fix is II.53: a recorded version is never fed back to a manager that cannot replay one, so `lock` keeps recording on brew (drift detection needs only a version it can *read*) and nothing is ever built into `tokei@14.0.0`. `brew.rs` stops constructing `name@version` at all. A pin somebody *typed* on such a manager is refused at plan time by name, and is fatal under `--locked`. **The family, swept:** fourteen backends dropped a declared `@version=` silently; `pkgin`, `pkg` and `pkg_add` take a plain `name-version` and were built, taking the unbuilt ceiling from four to zero, and `xbps` joined the permanent cannot-pin list because its operand needs a revision suffix `@version=` does not carry — building `name-1.2.3` would have been the same "build a name and hope" that `brew` was doing. **And the gate itself was the wider bug:** it scanned `registry.rs` and `builtin_backends.toml`, so it could not see `brew` — a hand-written backend — at all, nor the eleven others in that blind spot. It now covers them. **As reported:** Measured on the macOS nightly leg, 2026-08-10: `shall lock` records `tokei = "14.0.0"` (tokei's real version, correctly read); `resolver.rs:1146` injects that into every later spec as `@version=`; `brew.rs:75` turns `@version=` into `format!("{name}@{version}")`; brew answers `No available formula with the name "tokei@14.0.0"` and the whole sync dies. **`name@version` is not a version selector in Homebrew - it is a different formula's name.** Versioned formulae exist for a handful of packages and carry a series (`python@3.12`, `openssl@3`), never a full semver, so the constructed name will essentially never resolve. The comment above the line claims *"Only some formulae publish versioned variants; otherwise brew installs latest"*; brew does no such thing, it errors. This is E13's family - an argv Shall constructs perfectly and the tool refuses - with the aggravation that **the user never typed the pin**: Shall wrote it into its own lockfile and then choked on it. **The survey around it took three passes to get right, and the corrections are the useful part.** The first attribution walked to the nearest `name:` above each `version_pin` and crossed function boundaries, blaming `xbps` and `dotnet` for lines belonging to the AUR helper and to `base_config`; the second missed `cfg.version_pin = Some(...)` and reported `dnf` and `conda` as dropping the pins they declare; the third put `helm` among the managers that could pin, when Shall's `helm` backend installs *plugins from a URL* and the source carries its own version. **The settled count: fourteen backends drop a declared `@version=` and report success, under thirteen ledger entries - `yay` and `paru` share one registrar.** Ten cannot do otherwise - `pacman` and the AUR helpers (rolling repositories publish one version), `scoop` (versioned manifests, not a flag), `mas` (the store serves the current version), `macports` (the version is in the Portfile), `krew`, `slackpkg`, `eopkg`, `emerge` (Portage needs `=category/name-version`, and a bare version cannot be made into an atom). **Four are live defects**: `xbps`, `pkgin`, `pkg` and `pkg_add` all take a `name-version` operand and Shall does not send one. `xbps` is the one that shows why a comment is not an instrument - it never names `version_pin` at all, takes `Default`'s `None` in silence, and a scan looking for the literal `version_pin: None` calls it clean. `a_version_pin_is_honoured_or_explained_tests` now requires every registrar that builds a `ManagerConfig` and every row of `builtin_backends.toml` to pin or to carry a reason, splits "cannot" from "not built yet" so the second is a ratchet under a ceiling, refuses a ledger entry naming a backend that now pins or no longer exists, and shows its parser failing on all four shapes - including the two that fooled it. It reads the files rather than walking the registry, because registration is `cfg!(target_os)`-shaped and that is precisely how `dnf` went unaudited in `S83`. |
| **S86** | **FIXED 2026-08-10. The release could not be cut, and nothing anywhere said why.** `Build for x86_64-apple-darwin` asked for `macos-13`, the last Intel runner image, which GitHub has retired. **A job that names a label with no runner behind it does not fail - it queues, silently, with no error, no annotation and no log to open.** Measured: in run 31439543086 it sat 83 minutes in `queued` while the `macos-latest` row beside it started in three seconds; the same in every run that day. `Create Release` needs all four builds, so the release job never became eligible, and the only visible symptom was a run that never finished - which reads as slow CI, not as a blocker. Found by rehearsing the release through `workflow_dispatch` rather than by tagging, which is the whole argument for rehearsing it. Both Apple rows build on `macos-latest` now and Apple Silicon cross-compiles to x86_64 natively, so the target still ships. **What it costs, said rather than hidden:** an x86_64 test binary needs Rosetta to run on an arm64 host, so the cross row builds and does not test, and a step that says so prints in its place - a green tick that might have run the suite or might have run nothing is the vacuous check II.23 bans. The suite for that OS runs on the `aarch64-apple-darwin` row: same source, same tests, same OS, only codegen unexercised. `no_workflow_asks_for_a_runner_github_has_withdrawn` keeps a list of retired labels with what to use instead, because nothing in a workflow run will ever say it, and `the_withdrawn_runner_scan_sees_the_row_that_queued_for_ever` shows the predicate catching the shipped row and leaving both a live label and a label named only in a comment alone. **Third in a family this week:** `S80` published four assets all named `shall`, `S83` built one target of four, and this one built none of one - each invisible because the release path was reasoned about instead of run. |
| **S87** | **FIXED 2026-08-11, reproduced on demand after the register had it backwards. A cleanup uninstall reported success over a package it did not remove.** The cause is a durability mismatch between two files that must agree. Ownership is held in memory through a sync and serialised to `registry.json` **once, at the end, and only when the whole transaction succeeded**; the write-ahead log is written per operation, before and after each one. So a run killed after an install completed leaves the package installed, `Completed` in the log, and in no registry — and nothing put it right, because the entry is terminal so `heal` had nothing to replay, the package is present so no later sync reinstalled it, and drift removal only removes what Shall manages. **What the entry said before, and what was wrong with it.** It read *"the two that refused to uninstall were exactly the two `heal` recovered"*, and inferred from `SIGKILL left 2 newly-opened operation(s)` that one canary had completed normally. `_opened` is a **delta**, not a count of open entries — the harness's own comment says so — and the packages `heal` recovers are the ones that come back **owned**. It is the packages `heal` never touches, because their entries were already closed, that nobody owns. Reasoning from the field log had the two halves exactly the wrong way round, which is why two data points could not settle it. **The reproduction, and why the old harness could not produce it.** Both existing crash iterations poll the *filesystem* — `open` kills as the log opens an entry, `midway` once a canary reaches disk — and a canary reaches disk well before its entry closes, so both always killed too early; twelve iterations produced the failure zero times. Polling the **log** for a newly closed operation produced it on the first try: `void`, 2026-08-11, kill with `open=1 done=2`, then `3/3 on disk`, an empty registry, `heal` recovering only the one still-open operation, `why xbps:pv -> 'xbps:pv' is not under Shall management`, and `shall -y uninstall xbps:pv -> already up to date`, rc=0, `pv` still on PATH. The same kill a tenth of a second later — after the final write — left all three removable, which is the whole of the intermittency. **The fix, both halves.** (a) `heal` reconciles ownership from the **manifest**: every package this machine declares, that the registry does not carry and the manager still holds, is recorded and written, and the machine says so. Only `present` declarations count, so an `absent:` line is never claimed; an undeclared package is never claimed either, however it got there. It is called whether or not anything is interrupted — `needs_recovery` is false for a machine whose log is clean, and gating on it is what left the orphan orphaned through every later sync. (First built as a replay of the log's completed installs; ruled onto the manifest the same day — `Q55` — because the log expired at seven days and could not see a package installed by hand and declared afterwards.) (b) Owner ruling, `Q54`: `uninstall` no longer reports success over a package it did not remove. Both harnesses gain the `completed` iteration that produces this, and a check after every crash that no canary is left installed and unmanaged. |
| **S89** | **FIXED 2026-08-10. Three download backends read an entire response body into memory before writing it.** `web.rs`, `appimage.rs` and `github.rs` each called `.bytes()` on the response, so a URL that answered with something enormous - a redirect, or a server that simply never stops sending - was answered by allocating until something died, and there was no ceiling on the resulting file either. Found while sweeping `S88`'s family: the same shape, an outside party deciding how much of this machine Shall spends. Bodies now stream to disk chunk by chunk, which bounds the memory whatever the server sends, and are counted against `max_download_bytes` (2 GiB, `0` removes it). A declared `Content-Length` over the ceiling refuses before a byte moves; a chunked response declares nothing, so the running count is the actual bound. A body that goes over takes its partial file with it, because a half-downloaded artifact left on disk is one a later run can find and treat as complete - and the checksum that would catch that is exactly the one `@unverified` may switch off. Rule in II.55, reason in V.185. |
| **S88** | **FIXED 2026-08-10. Shall hung for fifteen minutes on a wrong sudo password, and again on a terminal with nobody at it.** **The premise every theory rested on was false: a closed stdin does not stop a password prompt.** sudo opens `/dev/tty` directly, so `Stdio::null()` guaranteed only that nobody would see the question — never that it would not be asked. The fix moves the asking into one place and takes it out of every other: credentials are primed once per run by a bounded, interactive `sudo -v`, and **every escalated command then runs `sudo -n`**, which can refuse and cannot wait. With no terminal to ask on, Shall says so at once. The prompt gets its own bound, `sudo_password_timeout_secs` (120s), because a package manager may legitimately work in silence for minutes and a prompt cannot — either somebody is typing or nobody is there. Rule in II.54, reason in V.184. **The family, swept:** `git` had the identical bug — a credential prompt read from `/dev/tty` under a comment asserting that closed stdin turned it into an error — and now carries `GIT_TERMINAL_PROMPT=0` and `GCM_INTERACTIVE=never`, the second because Git Credential Manager does not print a prompt at all, it opens a *window*. `ssh` was checked and deliberately left alone: the only way to set `BatchMode` from here is `GIT_SSH_COMMAND`, which overrides a user's `core.sshCommand`, so silencing a misconfigured remote would break every working custom transport. `app/fleet.rs` already passed `-o BatchMode=yes` and needed nothing. |
| **S90** | **FIXED 2026-08-17. The gentoo leg went from `pass=238 fail=0` to five failures and an exit 127 overnight, on a commit that touched nothing it runs.** The runtime stage is `FROM gentoo/stage3:latest`, and that tag is rebuilt daily — so the leg had been measuring whatever Gentoo happened to publish, and two separate assumptions about the base image expired on the same night. **(a) Portage without an ebuild repository.** Probed 2026-08-17 in a container: `/var/db/repos/gentoo` does not exist, portage says *"Section 'gentoo' in repos.conf has location attribute set to nonexistent directory"*, and `emerge --search '^jq$'` answers `[ Applications found : 0 ]`. Every check that asks Shall to resolve a bare name then fails with *"no package manager this line accepts has `jq`"* — the dry-run plan, all three chain forms, and `info` — which reads as a broken resolver and is an empty tree. **Shall was right the whole way through:** with the tree mounted back, `emerge --search '^jq$'` returns two ebuilds and bare `emerge jq` is refused by Portage itself as ambiguous, while Shall's own plan says `emerge:app-misc/jq`, which is the correct atom. The image now copies the tree from `gentoo/portage`, which is Gentoo's own published snapshot, and symlinks `/etc/portage/make.profile` under a `test -d` so a profile renumbering fails the *build* naming the directory instead of leaving the leg quietly measuring an unprofiled host. Portage's `IMPORTANT: N news items need reading` notice is suppressed in the same layer, because it is appended to the output of ordinary commands and this harness parses that output — via `news-gentoo.skip`, not `.read`, since `.unread` is regenerated from the tree on the next command unless the item is skipped. **(b) The argv gate could not run at all.** The probe step invokes `cargo test` inside the image; that night it answered `cargo: command not found`, exit 127. The image had carried `cargo` on 2026-08-16 and carried it again hours later — measured both times. The comment above that step said *"the image already carries a Rust toolchain"*, which was a claim about all four images in the matrix and false for two of them. `Dockerfile.guix` had already solved this for `metacall/guix`, which has never had `cargo`: build the suite in the Ubuntu builder stage and copy the binary in. Gentoo does that now, and `matrix.probe` drives `suite` rather than `cargo test`. **What the class is, and it is not "gentoo".** A leg whose ability to answer depends on a rolling upstream tag reports nothing on the days that tag disagrees, and reports it as a defect in Shall. `guix` was already immune by accident. Verified by building the image and running both CI steps locally rather than by pushing and waiting six hours for the nightly. |
| **S91** | **FIXED 2026-08-17. A coverage ceiling set ten wider than reality, so the gate could not fire; and the backend that walked through it.** `nixos:` was registered on 2026-08-16 and named in neither harness's `canary()` nor its `no_lifecycle_reason()`. On the macOS and Windows legs, whose `LIFECYCLE_GAP_CEILING` is 0, that is exactly the refusal the gate exists for and both went red by name — the gate working. On every container image it printed a `soft` line and passed, because that harness's ceiling was 11. **The ceiling had been left at 11 deliberately, and the file said what to do about it**: *"the soft branch prints each image's new count; take the largest of them from the next full matrix run and lower this line to it."* Nobody had. Read off eleven images' own soft lines rather than reasoned about — push run 31990107008 for ubuntu, arch, alpine, void, fedora, opensuse and storage, nightly 31992721802 for tools, slackware, guix and gentoo — every one printed `nixos` and only `storage` printed anything else (`zfs`, which genuinely has no canary on the only image where `SHALL_IT_STORAGE` is set). So the true maximum was 2, and 1 once `nixos` is named. A ceiling ten above the number it bounds is not a lax gate, it is an absent one: the next ten backends could each have arrived with no lifecycle and no stated reason and this line would have printed a caption each time. Lowered to 1, with the eleven measurements recorded beside it. |
| | **S88, as reported — kept because the reasoning in it is still right.** Two hard failures in the `tools` leg: *"sudo: a wrong password left Shall waiting 900s instead of reporting a failure"* and *"sudo: a terminal with nobody at it wedged Shall for 900s"*. 900 seconds is the harness's own timeout, so both are hangs without end, not slow paths - and they are what makes that job take 48 minutes. **Not introduced by `Q52`'s change from SIGKILL to SIGTERM**: the identical two sentences are in the scheduled run of 2026-08-09, before any of it, and the job has run 48 minutes and failed every night for at least six. Checked before it was claimed, because a hang appearing the same week a process-termination change lands is exactly the coincidence worth being suspicious of. This belongs to `Q52`'s family from the other end - not a child Shall abandons, but a child Shall waits on for ever - and it is the one remaining place where an outside tool can stop this program indefinitely with no message. It sat unread for the same reason `S84` did: a red nightly job nobody opens. |

## VI.3 Do not re-decide these

Three suspicions did not survive scrutiny:

- `matches!(b, "choco"|"scoop"|"winget")` at `generic.rs:363` is the **only** such site, and
  its comment is the best in the repo.
- `.unwrap()` density: 192 total looks alarming, but outside tests the max is **5 in one
  file** and ≤2 elsewhere. **Re-measured 2026-07-26 against a tree three times the size: 882
  in `src/`, of which 39 sit outside a `#[cfg(test)]` block** — and 24 of those 39 are in two
  files that are tests by content and not by attribute (`backends/link_teardown_test.rs` 22,
  `backends/artifact/real_releases_test.rs` 2). The genuine production maximum is still
  **5 in one file** (`core/executor.rs`) and ≤2 everywhere else, so the finding holds and the
  count that alarmed people never did. Do not re-open it; re-measure it if the ratio moves.
- `bisect`, `fleet`, `conflicts`, `generation` are real, unit-tested implementations, not
  stubs.

---
## VI.4 The first security bug, and the comment that hid it

**FIXED 2026-08-12.** `docs/GRADE-2026-08-12.md` B-1. Kept here because the *mechanism* by which
it stayed hidden is worth more than the fix, and the fix is four lines.

**A crafted package name ran an arbitrary program on Windows.** `shall search 'q&calc.exe&rem'`,
with any `.cmd`-shim manager in `priority`, launched `calc.exe` — measured, with a benign control
that did not. A bare unprefixed name in a shared module wrote arbitrary files by redirection, so
one line in a config somebody `shall add`s wrote files on every Windows machine that so much as
evaluated it.

**Two comments, each true on its own, and the pair of them false.**

`windows_shim_wrap` had two arms. The `.ps1` arm single-quote-escaped every argument and said so:
*"a crafted package name cannot break out of the string — no command-injection surface."* Correct.
The `.cmd`/`.bat` arm handed the arguments to `cmd /C` raw, under the comment *"Batch scripts are
plain-text; `cmd /C` runs them and forwards args cleanly."* `cmd` does not forward args cleanly —
it applies its own parsing to `>`, `<`, `|`, `&` and `^` before the batch file sees anything.

Meanwhile the validator's own doc comment carried the safety argument for the whole program:
*"no package-manager command is ever a shell string — every one is argv."* True everywhere except
the arm above, which is the counter-example that makes it false. **The two facts never met.** One
comment asserted a property; another comment, in another file, was the exception to it; and
neither had a reason to be read beside the other.

**The fix is a deletion.** `std` has recognised `.bat`/`.cmd` since 1.77.2 and spawns them through
`cmd.exe /e:ON /v:OFF /d /c` with per-argument escaping — returning an error rather than
mis-escaping an argument it cannot express. So the arm does not need to be fixed, it needs to not
exist: the resolved shim is handed to `std` as the program, and the escaping lives in one audited
place that is not this one.

**And the second layer, which is not the fix and must not be described as one.** Two strings
reached a manager before anything knew what kind of string they were — a bare name, probed
against each candidate manager to find out who owns it, and a `search` query, which is free text.
Both now pass a metacharacter gate. It is defence in depth. A comment calling a validator *the*
thing that stands between a crafted name and a command line is what let the first layer stay
broken for a year, so the gate's own doc comment says which layer it is.

**The habit, which is the part that is not fixed by a commit.** Every one of the three comments
was written by someone who had checked the thing they were writing about. The defect was in the
seam between them, and no seam has a comment.

---

## VI.5 A daemon's client on `PATH` is not a daemon running, and three tables believed it

**FIXED 2026-08-14, found by running the thing.** `service:` picked systemd inside every
Debian-family container, and every line failed:

```
Error: `systemctl` failed (exit 1): System has not been booted with systemd as init system
(PID 1). Can't operate.
Failed to connect to bus: Host is down
```

`systemctl` is installed in those images. systemd is PID 1 in none of them. And the machine had
`service` and `update-rc.d` sitting right there — a working SysVinit that the adapter table
never reached, because it stops at the first row whose `detect` command is on `PATH`.

**Not a container quirk, which is the part worth keeping.** It is every machine where a daemon's
client is installed and its daemon is not running: a chroot, WSL1, a Debian box on sysvinit with
systemd-shim, a host whose firewalld is stopped. The container is simply where somebody finally
ran it.

**Three tables shared the predicate.** `Detected::detect_command` is `init_providers.toml`'s,
`firewall_adapters.toml`'s and `setting_stores.toml`'s — the K17 mechanism, doing what it was
built to do, with a question that was wrong for all three. `firewall-cmd` present with firewalld
stopped is the same bug; so is `gsettings` present with no session bus.

**The fix is a second, optional predicate on the mechanism**, not a special case in the init
backend: `Detected::detect_file` names a path whose existence means the thing is *running*.
Only the systemd row uses it, and the path is not a guess — `/run/systemd/system` is what
`sd_booted(3)` checks. On a machine that has booted systemd nothing changes at all; where systemd
is merely installed, the row below it gets its turn. The other two tables get the seam and a
comment, because naming a liveness path for firewalld or dconf without measuring which one would
replace a wrong answer with an unfalsifiable one.

**And the search became testable on a named OS.** `first_present` read `std::env::consts::OS`
itself, so a *Linux* question could not be asked from this repository's Windows suite —
`adapter.rs`'s own header records that exact failure for four other copies of the OS filter, and
this was the fifth. `first_present_on(rows, os, …)` is the same move `AdapterRow::applies_to`
already made.

**What it says about the harnesses.** The hermetic suite could not have caught this: it drives
mock providers, and a mock's `command_exists` answers whatever the fixture says. Only a real
machine with `systemctl` installed and systemd not running could, and this repository has run one
on every push for months — with `service:` plan-smoked rather than driven, because its exemption
said starting a service needs an init a container does not run. The exemption was true of systemd
and false of SysVinit, and the two facts never met.

---

## VI.6 Three clients of one package database counted as three machines' worth of software

`pacman`, `yay` and `paru` are three programs over one libalpm database. `-Qe` from any of them
returns the same lines. Every surface in Shall that enumerates installed software across backends
keyed its answer on `backend:name`, so on an Arch machine one installed package was three
different packages — and the arithmetic that follows from that is not a display problem.

**Adopt manufactured a configuration that could not be synced.** `shall install jq` wrote one
line. `shall adopt` skipped `pacman:jq` because that line already declared it, then wrote
`paru:jq` and `yay:jq`, because each client was filtered separately and nothing had claimed the
name. `shall uninstall jq` then planned `remove 3`: pacman removed jq, and paru and yay were asked
to remove a package that was already gone. `error: target not found: jq`, twice, and the sync
failed. Forty of the arch harness's forty-five checks after that point failed on those two
declarations, none of them about what they were testing.

**The permanent one is `--absent`.** `shall uninstall jq --absent` asks which managers hold the
package and writes an `absent:` line per holder, deliberately — *"every holder is named, because
`uninstall jq` means the jq I have"*. On Arch that wrote three lines for one jq, and an `absent:`
line is not consumed by the sync that acts on it. The machine fails every sync from then on, and
deleting the line that "fails" does not help, because the failing removal is the *second* one.

**The other two were counting.** `shall list` printed 609 rows for 203 packages. The
installed-but-undeclared crawl tripled, which is the list `purge-undeclared` deletes from and the
ratio that guards that deletion.

**Why no test saw it.** Every one of these is correct on a machine with one client per database,
which is every host in the matrix except Arch — and the arch image only grew `yay` and `paru` when
it stopped running the harness as root, three weeks before this. The unit tests register one
backend; the property is about two backends agreeing, and a fixture with one of anything cannot
state it.

**The fix is a table, not a special case.** `READS_THE_DATABASE_OF` in
`backends/shared_database.rs` names the client and the database's owner. Everything else asks it:
adopt claims a name for the database rather than the client, the crawl and `list` collapse to one
row, `--absent` collapses to one holder.

**Which backend survives turned out not to be a constant** (`J3`, ruled 2026-08-16). The owner
was the first answer and it is right for a repository package: `pacman -Rs` removes an AUR
package that `yay` installed, so the surviving row is one the user can act on. It is wrong for
the *other* direction, which a manifest needs just as much — pacman cannot reinstall a package
that is in no sync repository, so a `pacman:` row for one is a line you cannot delete and put
back. So the owner wins the repository set and the helper wins the foreign set, which
`pacman -Qmq` names.

**The ordering lesson, which is the transferable part.** The claim has to be made before the
already-declared filter, not after it. Filtering first is what let the clients through: pacman was
dropped as "already managed" and left the name unclaimed for the two backends behind it. A dedup
that runs after a skip is not a dedup.

**And two more of the same family, found in the same run and not by looking for them.**
`shall update` fans its refresh out concurrently over `priority`, above a comment stating that a
refresh *"changes no package, so concurrent runs cannot contend on a package database"*. On Arch
that is wrong twice: all three clients take `/var/lib/pacman/db.lck`, and all three fetch the same
repositories. The container reported it plainly — `yay: could not refresh — failed to synchronize
all databases (unable to lock database)` — a command failing because it did one job three times at
once. `update` now refreshes once per database; a client's `-Sy` refreshes the owner's
repositories and none of its own, so nothing is lost by not asking it.

`shall upgrade` had the same collision through a different door. It partitions the managers into
a strictly-sequential half and a concurrent half, and the discriminator was `needs_root` — a good
proxy for "does this contend with another manager", and the AUR helpers are exactly where a good
proxy breaks. `yay` and `paru` are `needs_root = false` on purpose, because they must run
unprivileged and escalate internally, so they went into the *concurrent* half while `pacman` ran
in the sequential one, over the database all three of them lock. The partition now asks the
question it wanted rather than a proxy for it.

**The pattern worth carrying forward: a proxy predicate fails at the entry you added last.**
`needs_root` was true of every contending manager in this tree until two arrived that contend
without needing root, and nothing about adding them said "go and re-check what needs_root was
standing in for".

---

## VI.7 Eight system managers sorted below the language managers

`starter_order` makes exactly one distinction, and its own comment states it: *"a system manager
beats a language manager, because your distro maintains that build and updates it with everything
else."* It implements that with a hand-kept list of fifteen names, and everything not on the list
falls to a branch whose comment reads *"anything unrecognised sorts with the language managers
rather than ahead of them: a backend the onboarder added is not known to be safe to prefer over
your distro."*

Eight shipped system managers were taking that branch: `slackpkg`, `emerge`, `eopkg` and `guix`,
which are data rows rather than registrars, and `macports`, `pkg`, `pkg_add` and `pkgin`, whose
platforms have no image in the matrix. The caution in that branch is right and is about backends
*the onboarder* added, which nobody has vetted. A manager this project ships is vetted by
definition; leaving it there is not caution, it is an omission wearing caution's clothes.

**What it did.** On the slackware image, `init` wrote:

```
appimage
cargo
gem
github
go
setting
slackpkg
```

so `shall install bc` resolved to `cargo:bc` and failed with *"there is nothing to install in
`bc v0.1.17`, because it has no binaries"* — while `slackpkg search bc` had
`[uninstalled] - bc-1.07.1-x86_64-5` waiting. Every bare name on a Slackware, Gentoo, Solus,
Guix, MacPorts or BSD machine went to a language manager before the distro's own, which is the
exact inverse of the rule.

**The guard is a table that already knew the answer.** `docker/integration/run.sh`'s
`backend_for()` maps each distro image to the backend it is driven with, and that backend *is*
that distro's system manager, by construction. The test reads those arms and asserts each one
outranks `cargo`. Nobody has to remember to update a second list: adding a Solus image is what
makes `eopkg` required.

---

## VI.8 The lock file Shall could remove, would not look at, and therefore could not remove

pacman's `db.lck` is created mode `0000` and owned by root, and it is empty — it is a pure
existence lock, which pacman's own error says outright: *"if you're sure a package manager is not
already running"*, because it wrote no pid to check.

Both readers in `stale_lock.rs` took `&dyn Fn(&Path) -> Option<String>` and treated `None` as
*absent*. `None` is also what an existing file returns to a caller who may not open it. So on any
machine where Shall is not root, a stale `db.lck` read as **no lock at all**.

The two halves of the consequence:

- `find`, which `heal` uses, pushed it onto the *left alone* list: *"it is there and could not be
  read, so nothing can be proved about it"* — about a file with nothing in it to read.
- `held_for`, which the retry loop uses, filtered on "could it be read" and answered `Free`, so
  the sync backed off four times and reported `exhausted` instead of sending the user to `heal`.

Meanwhile `clear_stale_manager_locks` removes these with `rm -f` **through the elevating
executor**. The privilege to fix it was already there; the look that would have authorised it was
the one thing not elevated.

**Measured on the arch image, which is the only harness in the matrix that runs as a normal
user.** A SIGKILL mid-transaction left the lock behind and three checks failed on it —
`crash/open: the sync after the crash did not converge — still missing: pv dos2unix ncdu`,
`two-writers: the second run never recovered after the holder released`, and `crash-recovery
sync: the run after a killed holder failed — the lock was free, so this is a sync defect and not
a lock defect`. That last sentence is the harness diagnosing it correctly before anybody read it.

**The fix names the third state.** `LockFile::{Absent, Unreadable, Body}`. Unreadable is evidence
only where the contents were going to be the evidence — a pid file. For a lock answered by its
existence and the process list, demanding readable contents is asking for a permission in order
to ignore what it grants. The live-holder guard is untouched: a running `pacman` still wins,
readable or not.

**Why it survived this long.** Every other image in the matrix runs the harness as root, where
reading a 0000 root-owned file works. The arch image stopped running as root three weeks before
this — so that yay and paru could be driven at all — and the first fault-injection run under the
new user found it. The same commit that unlocked two backends' coverage exposed a defect in a
third area entirely, which is the argument for changing one thing about a harness and then
reading everything it says.

## VI.9 A terminator sent to a shell script, and the empty answer nobody could see

`core/argv.rs` records, per binary, whether Shall may put a `--` between the subcommand and the
package names. `slackpkg` was listed as terminating under `GETOPT`, whose own text says what kind
of claim that is: *"delegates option parsing to getopt(3)/argp, where `--` is the terminator by
definition. Inferred from the parser it links against, not asked of the tool itself."*

slackpkg links against nothing. It is a shell script, and it reads `$1` as the pattern.

Measured in the slackware image, 2026-08-14:

```
$ slackpkg search -- bc
search: Ignoring extra arguments: bc
Looking for -- in package list. Please wait... DONE
No package name matches the pattern.
$ echo $?
0
```

**Exit 0, empty result, nothing on stderr.** So `Searchable::search` returned `Ok(vec![])` and
every caller read it as *slackpkg does not carry this package* — a wrong answer with the shape of
a right one. `shall search bc` printed cargo and gem rows and no slackpkg row; the trace says
`Backend 'slackpkg' returned 0 results` with no error beside it.

**What it cost is the second half of VI.7.** Ranking slackpkg above the language managers is
necessary and does nothing on its own: resolution asks who carries the name, and slackpkg was
answering *not me* about a package sitting in its own list. So `shall install bc` still became
`cargo install bc` on a machine with `bc-1.07.1-x86_64-5` one subcommand away. Two independent
defects produced one symptom, and fixing either alone leaves it standing.

**`install` hid it.** `slackpkg install -- bc` installs bc, because install takes a *list* of
patterns and quietly drops the one that matched nothing. Search takes exactly one, and the
terminator is that one. A row is one boolean per binary, so a verb that survives the terminator
says nothing about the verb beside it — and the surviving verb is the one anybody would test.

Removing the terminator opens no injection hole: a name beginning with `-` is refused by
`Validator` and again by the grammar, before any argv is built.

**The gate that will not miss the next one.** CI runs `terminator_probe_tests` against each
distro image's own managers, so this row was unasked only because no Slackware image existed to
ask it. Wiring slackware into that matrix arms it. Every remaining `GETOPT` row belongs to a
manager with an image (asked nightly) or to one with no image at all — `emerge`, `guix`, `port`,
`pkgin`, `pkg`, `pkg_add`, `pkg_delete`, `eopkg`, `pamac` — which is the same list as the
unproven-lifecycle table, and for the same reason.
