# Session handoff — 2026-08-05

**To resume:** read this, then `docs/FINDINGS-2026-08-05.md` (the evidence), then the OPEN entries
`Q32`–`Q38` in `docs/spec/decisions.md` (what needs the owner). `docs/INEFFICIENCIES.md` gained
`I-48` and `I-49`.

**One line:** the Windows stall was three different bugs wearing the same costume; chasing it with
an instrument instead of a theory turned up a larger product gap underneath — sixteen backends
running with no exit policy at all.

---

## 1. Where the work started

Prior sessions had four observations of a wedged Windows sweep (idle `linix`, frozen log, resumes
when killed) and three explanations, all three later refused by measurement. The agreed next move
was **one instrumented full run that captures the wedged process's child list before anything
kills it.**

## 2. What is now true

- **The stall is diagnosed** — as three separate causes, not one.
- **The Windows sweep ran to completion unattended for the first time**: `pass=234 fail=13
  soft=23`. Every previous run ended with a human killing something.
- **A larger product gap was found underneath it** and is fixed (§4).

## 3. The three causes

| capture | shape | cause |
|---|---|---|
| `install nimble:nimjson` | 0 CPU, tree of 1, orphaned `nim.exe` at `PPID 0` | **Q32** — the idle bound covers `child.wait()`, not the read of the output. An orphan holding the pipe sets the duration, unbounded. Reproduced: 64s against a 20s bound, **exit 0, reported SUCCESS**. |
| `install pnpm:json` | 78ms CPU, live child | not a wedge — genuinely slow. |
| `heal` | 0 CPU, tree of 1, **no orphans** | **Q37** — an in-process GitHub download with no whole-request timeout, of an artifact it was always going to refuse. 60.9s + 119.1s of a 201s run. |

**My own theory was wrong too.** I proposed that LiNix hands the terminal to a child which then
blocks on stdin, with a real measurement behind it (48ms without a terminal, 21.9s with one). The
capture refuted it inside minutes — the wedged process had no child at all. Kept as `Q35` because
the hazard is real; it is not this bug.

## 4. The finding that outranks the stall — FIXED

`with_manager_policy` attaches a manager's `ExitPolicy`. `register_generic` applies it; **sixteen
registrars call `reg.register` directly and never did**: `apk apt aur_helper bun choco dotnet gem
macports mas pip pkg_add_openbsd pkg_freebsd pkgin scoop winget zypper`.

So those backends ran on `ExitPolicy::default()` — no failure, permanent or absent markers.
Measured before the fix:

```
scoop install <nonexistent>  ->  linix-failure-class: unknown   ->  still declared
```

Q1 (2026-07-27) withdraws a declaration when the failure is permanent. It never fired. The dead
line then failed **every later install** — ten of the run's thirteen failures, plus `rollback`,
`activate`, and the lifecycle ratchet collapsing to 1 of 5. Worse and never exercised: scoop's
policy exists because *scoop exits 0 on every outcome*, so without it a failed scoop command reads
as a success.

**After the fix:** `linix-failure-class: permanent`, declaration **withdrawn**.

**The guard test had been green the whole time and could not fail** — it called
`with_manager_policy(core)` *inside its own assertion*, proving the helper works and never asking
whether a registrar calls it. Replaced with a source scan (the `prompt_guard_tests.rs` idiom,
because the defect is a missing line), proved to fail by removing one fix and watching it catch it.

## 5. Decisions I made without asking, and why — all reversible

1. **Harness gets `exec < /dev/null`.** An automated sweep must not hand a keyboard to something
   with nobody at it. Invisible outside the program, so mine per CLAUDE.md.
2. **…with `LINIX_IT_KEEP_TTY=1` to put it back.** A fix that also deletes the only way to
   reproduce a bug leaves nobody able to show it existed, or to notice it returning.
3. **The stall capture photographs and never kills.** Killing is what destroyed the evidence four
   times. It samples CPU across a window, because one frame cannot tell slow from stuck — and two
   of the three captures would have been misread without that.
4. **Fixed all sixteen registrars, not the three Windows ones I had measurements for.** "Fix the
   family" is binding. It restores ruled behaviour (Q1) rather than inventing any. **This is the
   broadest change and the one to reverse first if something regresses off-Windows** — on `apt`,
   `zypper`, `apk`, `pkg_freebsd` I have the identical code shape and no run.
5. **The Q32 test is committed `#[ignore]`d.** It fails correctly; Q32 is OPEN and its fix changes
   when a command reports failure. Better a named ignored test than a red tree or a silent gap.
   **Remove the ignore in the same commit as the fix.**
6. **Did not fix Q32/Q33/Q37 themselves.** All change behaviour a user notices and carry register
   IDs. Recommendations are written; the rulings are the owner's.
7. **Raised the harness-vs-product disagreement rather than picking a side** (does a failed
   install's name leave the manifest). `GRADE-2026-07-28` E2 says that assertion was deliberately
   pointed at the product. It turned out the product was right to keep it *and* the classification
   was broken — §4 is why.

## 6. Speed (the 2026-08-02 ruling: as parallel, efficient and fast as possible)

Same host, LiNix's own `--timings`:

| command | wall | child commands | overlap | waves |
|---|---|---|---|---|
| `sync --dry-run` (466 declarations) | 2.65s | 21 → 10.35s | **3.9x** | 2 |
| `check health` | 6.6s | — | — | — |
| `list` | 3.2s | — | — | — |
| `--version` | 0.1s | — | — | — |
| **`heal`** | **205.14s** | 27 → 33.31s | **0.2x** | **27** |

The engine is fine; `heal` is not part of it. 27 waves for 27 commands is fully serial, and each
call is `install(std::slice::from_ref(spec))` — one package per command, so I-1's batching is
bypassed too. 23 of 30 attempts were the same package. Written up as **I-48**; **I-49** is the
download-before-check. I-48's correct fix is to route recovery through the transaction engine
rather than parallelise a second copy of it — and the loop deciding *which* entries to run is
`Q33`, which is OPEN and is the same loop. **They should land together.**

## 7. Verified vs not

**Verified:** `cargo build --all-targets`, `cargo test --no-fail-fast` (**1617 passed, 0 failed, 1
ignored**), `cargo clippy --all-targets` — all clean. The policy fix confirmed end-to-end on the
release binary. Q32 reproduced deterministically. `heal` reproduced three times (189/193/205s).
The new scan test proved to fail.

**Not verified:** the sixteen-registrar fix on non-Windows platforms (code shape only).
`appimage.rs`'s place in the I-49 family is by shape, not by running it. `choco` was never
exercised — the shell was not elevated, and elevation is available here, so that is an avoidable
coverage gap. Of the 13 sweep failures I explained 10–11: `activate converges onto the named
profiles` is attributed to the cascade but **not proven**.

## 8. Next, in order

1. **Rule `Q37`** — one-line ordering fix, 180s per `heal`, and the reason three stalls were
   misread.
2. **Rule `Q32`** — unbounded, silent, reports success. Remove the test's `#[ignore]` with it.
3. **Re-run the Windows sweep** now that §4 is fixed, and see how many of the 13 failures survive.
   That is the cheapest confirmation available and it proves or kills the `activate` attribution.
4. **`Q33` + `I-48` together** — which entries recovery acts on, and running them through the DAG.
5. **`Q36`** — `adopt` writes 186 winget declarations whose identity embeds a version; one decayed
   inside 76 minutes. Self-regenerating on any real Windows machine.
6. `Q34` (reduced to "the error names the innocent package"), `Q38`, `Q35`.
7. Harness leftovers: the summary's stall excerpt greps too loosely and prints `bash` noise; run
   `choco` from an elevated shell.

## 9. How to reproduce anything here

```bash
# Full sweep in a real console, capture armed, stall reproducible:
LINIX_IT_KEEP_TTY=1 LINIX=./target/release/linix.exe ./scripts/integration-windows.sh scoop jq
# Snapshots land in $TMPDIR/linix-it-win-stall/stalls.txt; the summary names them.

# Where heal's time goes — timestamp every line and read the gaps:
linix heal 2>&1 | while IFS= read -r l; do printf '%s %s\n' "$(date +%s.%N)" "$l"; done
```

`scripts/stall-snapshot.ps1` can be run standalone against any suspected wedge:
`powershell -NoProfile -ExecutionPolicy Bypass -File scripts/stall-snapshot.ps1 -OutFile out.txt`.

## 10. The method note, because it is the transferable part

Everything here came from measurement refusing a story. Four theories were produced by reading
code — three before this session, one mine — and all four were wrong, while 1617 tests stayed
green throughout. Several defects had a test written *specifically for them* that could not fail;
two of those I wrote myself this session (a fixture Windows rejected for bad quoting, so the test
passed in 0.06s proving nothing; and a probe whose `pgrep -f linix.exe` never matches a native
process from msys, so it took zero snapshots and said nothing was wrong).

**The recurring defect class in this repo is a check that cannot fail.** Watch it fail first.
