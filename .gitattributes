# Shell scripts must keep LF endings even on Windows checkouts.
#
# They run inside Linux containers, where /bin/sh is dash. A CRLF file makes dash read
# line 1 as `set -u<CR>` and abort with "set: Illegal option -" before a single test runs.
# Windows sets core.autocrlf=true by default, which rewrites text files to CRLF on
# checkout — so without this, the integration harness is broken on any Windows clone.
*.sh text eol=lf

# Same reasoning: these are read by Linux tooling inside the images.
Dockerfile* text eol=lf
*.bash text eol=lf

# Git requires a hook to be named for the hook, so this one cannot carry `.sh` and the glob above
# does not reach it. Same failure as that glob's, one directory over and worse: a CRLF `set -eu<CR>`
# makes the hook exit non-zero on every commit for a reason that has nothing to do with the commit,
# and the first thing anyone does about a hook that always refuses is stop using hooks.
.githooks/* text eol=lf

# Bind-mounted into the containers and parsed there, so it is in the same class as the scripts
# even though it is data. `awk '{print $2}'` over a CRLF line yields `7<CR>`, which is not an
# integer to `[ -lt ]` — the test errors, the shell takes the else branch, and the else branch is
# the one that reports the ratchet satisfied. Every other file mounted by `run.sh` and `ci.yml`
# was already covered by `*.sh`; this one was the exception, on a repo whose Windows clones get
# core.autocrlf=true.
scripts/lifecycle-floor.txt text eol=lf

# A parser fixture's line endings ARE the content under test. These files are captured from a
# real manager's stdout and compared byte for byte by the parser that reads that manager; letting
# a Windows clone rewrite them to CRLF changes the input and can only change the answer — a
# `\r` on the end of every version string, or a row the parser stops recognising. The reasoning
# is `scripts/lifecycle-floor.txt`'s, one directory over, and the file that made it apply here
# was committed with `warning: LF will be replaced by CRLF the next time Git touches it`.
tests/fixtures/** text eol=lf

# Markdown, because the spec is read by tests as well as by people. Four files in `docs/spec/`
# were CRLF in a working tree whose `docs/SPEC.md` was LF, and the difference decided which
# gates ran: `grammar_table_matches_the_spec_tests` splits target-state.md on "```\n", a
# sequence that does not occur in "```\r\n", so it failed with *"no fenced block follows ..."*
# — a message that blames the heading for moving. Its sibling
# `backend_count_matches_the_spec_tests` reads SPEC.md, was LF, and passed. Two gates over the
# same spec, one of them dark, and nothing said so. CI is Linux and never saw it, which is the
# `*.sh` entry's story exactly: broken only on the machine the project is written on.
*.md text eol=lf
