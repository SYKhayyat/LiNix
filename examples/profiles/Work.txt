# profiles/Work.txt — a named set of modules you turn on and off live.
#
# Profiles are Capitalized; modules are lowercase. Several profiles can be active at
# once and their sets are unioned, so `shall activate -a oncall` adds without
# disturbing what is already on.

use base
use workstation
